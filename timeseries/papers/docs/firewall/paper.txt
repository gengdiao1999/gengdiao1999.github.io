IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017                                                           1699




                          Firewall Fingerprinting and Denial
                                of Firewalling Attacks
                      Alex X. Liu, Amir R. Khakpour, Joshua W. Hulst, Zihui Ge, Dan Pei, Jia Wang

   Abstract— Firewalls are critical security devices handling all              accordingly; second, we need to know how attackers can
traffic in and out of a network. Firewalls, like other software                possibly attack a firewall over the Internet.
and hardware network devices, have vulnerabilities, which can                     In this paper, first, we investigate some possible firewall
be exploited by motivated attackers. However, just like any
other networking and computing devices, firewalls often have                   fingerprinting methods and surprisingly found that these meth-
vulnerabilities that can be exploited by attackers. In this paper,             ods can achieve quite high accuracy. Second, we study
first, we investigate some possible firewall fingerprinting methods            what we call Denial of Firewalling (DoF) attacks, where
and surprisingly found that these methods can achieve quite                    attackers use carefully crafted traffic to effectively overload
high accuracy. Second, we study what we call denial of fire-                   a firewall. To our best knowledge, this paper represents
walling (DoF) attacks, where attackers use carefully crafted
traffic to effectively overload a firewall. To the best of our                 the first study of firewall fingerprinting and Denial of Fire-
knowledge, this paper represents the first study of firewall                   walling attacks. The attack presented in this paper can be
fingerprinting and DoF attacks.                                                used in conjunction with the attack presented in Qian and
   Index Terms— Network Security, firewalls.                                   Mao’s work [3]. Designing countermeasures for firewall fin-
                                                                               gerprinting and Denial of Firewalling attacks is out of the
                                                                               scope of this paper, but is the next step of this line of
                          I. I NTRODUCTION                                     research.
A. Motivation                                                                  B. Limitation of Prior Art

T     HE security and reliability of firewalls are critical because
      they serve as the first line of defense in examining all traf-
fic in and out of a network and they have been widely deployed
                                                                                 Prior art mostly focused on operating system fingerprint-
                                                                               ing [4]–[9]. Many tools such as NMAP [4] have been
                                                                               developed to identify a target host’s operating system using
for protecting both enterprise and backbone networks. How-                     TCP and UDP response characteristics. There are several
ever, just like any other networking and computing devices,                    approaches to finding out the operating system ranging from
firewalls often have vulnerabilities that can be exploited by                  simple banner observation to highly complicated TCP, UDP
attackers [1], [2]. To exploit firewall vulnerabilities, the first             and ICMP-header analysis. However, none of these methods
step that attackers need to do is firewall fingerprinting, i.e.,               can be used for firewall fingerprinting because firewalls, like
identifying the particular implementation of a firewall includ-                other network middleboxes, forwards the traffic and cannot
ing brand name, software/firmware version number, etc. For                     be targeted directly. For security purposes, some firewalls are
example, in the seminal work by Qian and Mao [3], the attacks                  configured in bridge mode with no IP address to be remotely
discovered by them assumes that the attackers knows the                        accessible by the administrator. Hence, such approaches can-
particular implementation of the firewall under attack. On the                 not be effective for firewall fingerprinting.
defense side, we first need to know how attackers possibly can
fingerprint a firewall so that we can design countermeasures                   C. Technical Challenges
   Manuscript received June 22, 2014; revised October 18, 2014; accepted          This work has three major technical challenges. First,
October 26, 2014. Date of publication February 13, 2017; date of current       finding the firewall implementation characteristics that we can
version May 3, 2017. This work was supported in part by the National           use for fingerprinting is difficult because firewalls are mostly
Science Foundation under Grant CNS-1318563, Grant CNS-1524698,
Grant CNS-1421407, and Grant IIP-1632051, in part by the National Natural      closed source and it is difficult to infer any implementation
Science Foundation of China under Grant 61472184, Grant 61373129, and          details from them. Moreover, there are many parameters and
Grant 61321491, and in part by the Jiangsu Innovation and Entrepreneurship     configuration details that can affect the performance of a
(Shuangchuang) Program. This paper was presented at the Proceedings
of the 31th Annual IEEE Conference on Computer Communications                  firewall. Second, inferring the type of a target firewall is
(INFOCOM), pages 1728–1736, Orlando, Florida, March 2012. The                  hard for attackers as they have no remote access to the
associate editor coordinating the review of this manuscript and approving it   firewall. Third, finding effective attack strategies on a firewall
for publication was Prof. S. Xu. (Corresponding author: Alex X. Liu.)
   A. X. Liu, A. R. Khakpour, and J. W. Hulst are with the Department          is difficult. Knowing some performance characteristics is not
of Computer Science and Engineering, Michigan State University,                enough for designing effective attacks. Furthermore, for dif-
East Lansing, MI 48824 USA (e-mail: alexliu@cse.msu.edu;                       ferent firewall products, the DoF defense mechanisms may be
khakpour@cse.msu.edu; hulstjos@cse.msu.edu).
   Z. Ge, D. Pei, and J. Wang are with AT&T Labs–Research, Florham Park,       different.
NJ 07921 USA (e-mail: gezihui@research.att.com; peidan@research.att.com;
jiawang@research.att.com).                                                     D. Our Approach
   Color versions of one or more of the figures in this paper are available
online at http://ieeexplore.ieee.org.                                             In this paper, for the first time, we propose a set of
   Digital Object Identifier 10.1109/TIFS.2017.2668602                         techniques that can collect some information about a firewall
                      1556-6013 © 2017 IEEE. Personal use is permitted, but republication/redistribution requires IEEE permission.
                           See http://www.ieee.org/publications_standards/publications/rights/index.html for more information.
1700                                                IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017



using packet processing time of probe packets and use the            Section III. In Section IV, we present an overview on different
collected information to identify the particular implementation      steps of our study on firewall fingerprinting and describe our
of the firewall. For our study, we build a testbed consisting        testbed specifications. Section V presents the results of our
of three popular firewalls (including an open-source firewall        experiments for identifying firewall implementation charac-
and two enterprise firewalls [10]), two computers hosting eight      teristics. We present methods for inferring the implementa-
virtual machines for generating traffic to the firewalls, and one    tion of a remote firewall in Section VI. We examine the
computer for sending probe packets and measuring the firewall        effectiveness of our designed attacks on different firewalls in
processing time of the probe packets. Because firewalls are          Section VII. We conclude our studies and present future work
very expensive and our budget was limited, we could only             in Section VIII.
support three firewalls in our testbed. Moreover, due to privacy
and legal reasons, we are obligated to keep the brand and                                   II. R ELATED W ORK
model of firewalls confidential.
   We address the three technical challenges as follows.                To the best of our knowledge, this is the first study
To address the first challenge, we measure packet processing         of firewall fingerprinting and Denial of Firewalling attacks.
time to identify the type of packet classification algorithms in     Salah et al. [12] propose a method of attack on firewalls that
use by the three firewalls, sensitivity of firewall performance to   perform sequential search. The basic idea is to send packets
traffic load, and some other characteristics of firewalls. We use    that match the last rule in a firewall. However, it is extremely
four different techniques to send probe packets to identify the      difficult, if not impossible, for an attacker to find the packets
type of caching mechanism that a firewall uses in both stateful      that match the last rule in a firewall without knowing the policy
and stateless modes. We also examine the sensitivity of firewall     and implementation of the firewall. Furthermore, our results
performance to transport protocol types (i.e., TCP or UDP)           actually show that attack traffic consisting of accepted packets
and packet payload size. To address the second challenge, we         is more effective than attack traffic consisting of the packets
first study firewall decisions for TCP packets with unusual          discarded by the last rule.
flags to find fingerprints for different firewalls. We also use         Work has also been done on firewall performance evalua-
machine learning techniques to identify firewalls using features     tion [13]–[16]. Lyu and Lau [13] measured the performance
extracted from probe packet processing time. To address the          of a firewall under seven different policies, where each policy
third challenge, we put the firewalls under different attacks        is for one security level. In a similar vein, Funke et al. [14]
and compare probe packet processing time under each attack.          evaluated the firewall performance (mostly firewall through-
We then identify the most effective attack strategy based on         put) under policies with differing number of rules. They also
the magnitude and standard deviation of the processing time          show that more rules do not necessarily imply poorer firewall
of probe packets.                                                    performance.
                                                                        There are some industrial reports on comparing com-
E. Assumptions                                                       mercial firewalls in terms of performance under different
                                                                     circumstances. Bosen in [15] compared Secure Computing
   We have three assumptions. First, we assume that the              Sidewinder [17] with Checkpoint’s NGX [18] and reported
attacker has access to a compromised host inside the network         better throughput for Sidewinder when high-level of protection
(behind the target firewall) to measure packet process time and      including packet and protocol inspection is required. Tolly
decisions of the firewall. This can be achieved by host attack-      Group, one of the independent test labs that performs extensive
ing methods such as infecting a host through web browsing.           tests on different IT devices from different vendors, compared
In general, this assumption is needed for firewall attacking         independent Checkpoint Firewall (VPN-1 Pro), PIX Fire-
methods such as Qian and Mao attacks [3]. Second, we assume          wall 535 [19], and NetScreen-500 [20]. The report indicated
that we can accurately measure the packet process time.              that the Checkpoint Firewall outperform the other two firewalls
This can be achieved using latency measurement methods               in most of the tests run [16].
such as COLATE [11]. Third, we assume that the attacker
can build a similar testbed as ours consisting of as multiple
different firewall models from different manufacturers to build                             III. BACKGROUND
classification models. Note that this is a one time cost.            A. Firewall Policies
                                                                        For each incoming or outgoing packet, a firewall decides to
F. Key Contributions                                                 accept or discard it based on its policy. A firewall policy is
  We made three key contributions in this paper. First, we           composed of a sequence of rules, where each rule specifies a
identified firewall implementation characteristics that one can      predicate over five different fields: source and destination port,
evaluate for black box firewalls. Second, we proposed methods        source and destination IP address, and IP protocol. Typically,
for inferring the implementation of a target remote firewall.        firewall policies do not check the source port field. The rules
Third, we identified some attack strategies for overloading an       in a firewall policy may overlap and conflict. To resolve
identified remote firewall.                                          conflicts, firewalls follow the first-match semantic, i.e.,, the
  The organization of the rest of the paper is as follows.           decision of the first rule that a packet matches is the decision
We first give an overview of related work in Section II.             of the firewall for the packet. An example firewall policy is
We then provide background information on firewalls in               in Table I.
LIU et al.: FIREWALL FINGERPRINTING AND DoF ATTACKS                                                                                1701



                             TABLE I                                 given packet, the hardware circuit of the chip compares the
                  A N E XAMPLE F IREWALL P OLICY                     packet with every stored rule in parallel and returns the
                                                                     decision of the first rule that matched the packet. TCAM
                                                                     based packet classification is widely used in high performance
                                                                     routers and firewalls because the lookup is done in constant
                                                                     time.

                                                                                             IV. OVERVIEW
B. Caching and Statefulness                                          A. Roadmap
   One method of increasing firewall performance is to cache            To study DoF attacks and defenses, we design a testbed with
rules or flows based on temporal locality. Rule caching stores       three popular firewalls for conducting extensive experiments
the four-tuple of source IP, destination IP, destination port, and   and performance measurements. Of the three firewalls tested,
protocol type for packets that a firewall has performed a full       two are software firewalls while the other is hardware based.
lookup on its policy for. The decision associated with each          A software firewall is implemented fully in software and
entry is stored in the cache.                                        may reside on a multipurpose machine as one of many ser-
   When a firewall with rule caching receives a packet, it first     vices being provided. Typically, software firewalls are highly
checks whether the four-tuple header of the packet is in its         configurable and offer more customization and services than
cache; if found, the decision for the packet can be made             their hardware counterparts. Hardware firewalls are made
without checking the packet against the main firewall rules;         specifically tailored for packet classification. Generally, they
if not found, the firewall checks the packet against its policy      are more limited in capabilities than software firewalls but are
and then caches the four-tuple of the packet with the decision.      usually very fast in classification as they are purpose built.
Flow caching stores the five-tuple, which includes the source           Our measurements are mostly based on probe packet
port field in addition to the four fields used in rule caching.      processing time taken on remote hosts before and after a fire-
The lookup process for flow caching is similar to that for           wall. In our initial experiment we study firewall characteristics
rule caching. The purpose of flow caching is to have a fine-         induced by their implementation. We examine firewall packet
grained access control beyond firewall policies. For example,        classification algorithms to understand whether or not they
to protect against SYN flooding attacks, some firewall products      use sequential search for packet filtering. We then measure
stop accepting new SYN packets with new source ports when            the sensitivity of firewall packet classification algorithms to
they see too many open flows from a specific source address          firewall background traffic load. We continue our studies by
with different source ports.                                         inspecting the firewall caching techniques and specifying their
   Commercial firewalls often support both stateful or stateless     caching effectiveness. We finalize our study by looking at
modes. A stateful firewall tracks TCP sessions in a state table      firewall processing time with respect to probe packet payload
by examining the TCP flags of incoming TCP packets. This             size to understand if they have an impact on the firewall packet
ensures that the packet in a TCP session follows the correct         processing time (PPT).
order that includes a proper handshake and tear-down. The               The second experiment is to determine if an attacker can
firewall drops any packet with an illegitimate flag. After a         infer the implementation of a firewall remotely by sending
correct handshake, an entry is made in the state table. The          probe packets through the firewall. The firewall implementa-
packets that match the session entries bypass the firewall. Once     tion inference process is studied from two perspectives. First,
a session goes through the correct termination procedure, its        we try to find a signature for each firewall based on the
table entry is removed.                                              decision of a sequence of TCP packets with an unusual set
                                                                     of flags. The results show that the three firewalls, especially if
                                                                     they are in stateful mode, discard TCP packet sequences with
C. Packet Classification Solutions                                   unusual TCP flags. As administrators rarely define policies
   The process of checking a packet against a firewall policy        on TCP flags, the obtained signature usually has a close
is called packet classification. Packet classification solutions     association with the firewall implementation. An attacker can
fall into two main categories: software based solutions and          use this signature to infer firewall implementation remotely
Ternary Content Addressable Memory (TCAM) based solu-                with high confidence. Second, as a complementary method, we
tions. Software based packet classification solutions include        use PPT of a sequence of probe packets to train a classification
the simple sequential search algorithm and other algorithms          model for each firewall and use it accordingly to infer the
based on complex data structures (e.g.,, [21]–[25]). The             implementation of a target firewall. Note that in the attacking
sequential search algorithm compares a packet with each              scenario, the attacker needs to build simple testbeds including
rule in a firewall policy sequentially until a match is found.       all speculated firewall brands to acquire signatures and the
Complex data-structure-based packet classification algorithms        classification model. He then needs to (1) compromise a host
include Recursive Flow Classification (RFC) [23], Aggregated         inside the network, (2) use security scanner tools such as
Bit-Vector [24], Tuple space [25], HiCut [21], and                   nmap [4] to find the packets that can go through the target
HyperCut [22], etc. For TCAM based packet classification,            firewall and reach the compromised host and (3) generate and
firewall rules are stored in a special memory chip; for any          send probe packets to measure their PPT. An attacker can
1702                                                IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017



                                                                     machine with 2 quad-core Intel Xeon 2.66GHz CPUs and
                                                                     16GB of RAM Each VMWare instance has four Linux virtual
                                                                     machines connected to each other by virtual switches. These
                                                                     virtual switches are connected directly (without an intermedi-
                                                                     ary switch) to each firewall (FW1, FW2, and FW3). The virtual
                                                                     machines on VM1 and VM2 are used to place background
                                                                     traffic load on the firewalls by sending a substantial amount of
                                                                     packets to different interfaces of the firewall. The traffic is gen-
                                                                     erated by Mausezahn network traffic generators (aka mz) [26],
                                                                     an open-source traffic generator. Using both VM1 and VM2,
                                                                     we are able to sustain a traffic rate of up to 300Mbps. Based
                                                                     on the design of experiments and attacks, the generated traffic
                       Fig. 1.   The testbed.
                                                                     can be accepted or discarded by the firewall to which it is
                                                                     sent. To put maximum load on the firewalls, the generated
also obtain more information using other monitoring tools
                                                                     traffic has no packet payload. This maximizes the number
(e.g., traceroute) to understand the number of hops and
                                                                     of packets that a firewall needs to process. If packets have
the extra delay between the probe packet sender and receiver
                                                                     payloads, firewall throughput will increase, but traffic packet
to create more accurate models for firewall implementation
                                                                     rate (i.e., packets per second) will decrease. As mentioned, the
inference.
                                                                     virtual switches are directly connected to the firewalls. This
   The third experiment evaluates which attack is more effec-
                                                                     is to separate the generated traffic for each firewall and make
tive for a given firewall. We compare probe packet processing
                                                                     firewall experiments independent from each other.
time when the firewall is under random DoF attacks and
                                                                        The last portion of the testbed is the Probe Machine & Traf-
some customized DoF attacks, which are designed based on
                                                                     fic Analyzer (PMTA): a Linux machine with Dual Quad-core
the firewall’s characteristics. The results indicate that some
                                                                     Intel Xeon 2.66GHz CPUs and 16GB of RAM. We send probe
firewalls can be easily overloaded while other firewalls are
                                                                     packets by PMTA directly (i.e., no switch in between) to each
fairly resilient to customized DoF attacks. Note that as it is not
                                                                     firewall using an open-source packet generator hping2 [27].
feasible to measure PPT for discarded packets in commercial
                                                                     If the probe packets are accepted by the target firewall they are
firewalls, all the measurement in this paper are based on
                                                                     routed back to PMTA through another interface (as it is shown
accepted packets.
                                                                     in Figure 1). In order to measure firewall packet processing
   We finally propose some techniques to protect firewalls from
                                                                     time, we use packet trace time-stamps. We use tcpdump [28]
DoF attacks by adding some dummy rules for TCP flags or
                                                                     to dump packets with time-stamps with microsecond reso-
adding hybrid queuing mechanisms to randomize the pattern
                                                                     lution. For the software firewalls (FW1 and FW2), we can
of the probe PPT. Indeed, firewall vendors can also use these
                                                                     analyze the packet traces and calculate the PPT based on
results and modify their implementation so that their products
                                                                     the difference of packet trace time-stamps of outgoing and
are more resilient against DoF attacks.
                                                                     incoming interfaces. However, the hardware firewall (FW3)
                                                                     does not support tcpdump or any traffic monitoring (i.e., packet
B. Measurement Environment                                           dumping) feature. Therefore, since we cannot measure the
   Figure 1 shows the testbed topology for our testing of            packet processing locally on the firewalls, the probe packets
three different firewalls. Firewalls FW1 and FW2 are software        are forwarded to PMTA and we calculate the time-stamp
firewalls running on a Linux machine with SMP kernel 2.6.            difference of the packet traces on PMTA. The time-stamp
Each firewall has 2 quad-core Intel Xeon 2.66GHz CPUs and            differences calculated on PMTA comprise the firewall PPT
16GB of RAM. FW3 is a hardware firewall that runs on a               plus probe packet round trip time (RTT) which in turn reduces
routing engine board with a 850MHz processor, 1,536MB                the accuracy of firewall PPT.
DRAM, and 256MB compact flash. Each firewall is config-
ured with the same policy comprised of 375 rules. The first                        V. F IREWALL C HARACTERISTICS
374 rules are set to accept traffic with the final rule discarding      To study firewall characteristics, we first give an overview
all traffic that is not specified previously. The firewall policy    on the methodology basics such as how the probe packets
is chosen from real-life firewall policies used in a university      are sent and how the PPT is measured by PMTA. We then
campus network. The rules are defined over four packet header        show the results for different firewall features containing
fields: source IP, destination IP, destination port number, and      firewall packet classification algorithm, firewall statefulness
protocol. As with most real-life firewall policies, only a few       and caching, and packets protocol and payload size impact.
rules overlap. Moreover, there is no rule hidden by another
rule (i.e., there is no rule with lower index that completely
covers a rule with higher index). Furthermore, the firewalls         A. Methodology Basics
are only configured for packet filtering; other services such as        The probe packets are sent by the PMTA in four modes as
VPN or NAT are disabled.                                             follows:
   In addition to the firewalls, the testbed has two machines,          • TCP Fix: A sequence of TCP packets with the same
VM1 and VM2, running VMWare ESX 3.5.0 on a similar                        packet header.
LIU et al.: FIREWALL FINGERPRINTING AND DoF ATTACKS                                                                                         1703




Fig. 2. The PPT for probe packets that match against a rule in the firewall policy. (a) Remote measurement on FW1. (b) Local measurement on FW1.
(c) Remote measurement on FW2. (d) Local measurement on FW2. (e) Remote measurement on FW3.

  •  TCP Vary: A sequence of TCP packets with the same                   box. However, we can design experiments to test (1) whether a
     packet header except the source port which is chosen                firewall adopts a sequential search based algorithm for packet
     randomly for each probe packet.                                     classification, (2) whether the performance of a firewall is
   • UDP Fix: A sequence of UDP packets with the same                    sensitive to its traffic load; and (3) how a firewall performs
     packet header.                                                      in terms of the PPT.
   • UDP Vary: A sequence of UDP packets with the same                      1) Using Sequential Search: To test if a firewall uses
     packet header except the source port which is chosen                sequential search for packet classification, we generate a
     randomly for each probe packet.                                     sequence of probe packets where each packet matches exactly
   We conduct two sets of experiments with and without                   one of the rules in the firewall policy. We then measure the
background traffic load in the testbed. The first set of exper-          PPT for the probe packets. If the PPT increases linearly as we
iments are performed under no background traffic load, i.e.,             progress further down the rule list, it is likely that the firewall
the probe packets are the only packets that are transmitted in           uses a sequential-search-based approach for packet classifica-
the testbed during the experiments. In contrast, the second set          tion. If the PPT exhibits a different change pattern or lack
of experiments are performed under background traffic load.              of change (i.e., remains flat), the packet classification algo-
In this case, all virtual machines send dummy packets with               rithm used by the firewall is not sequential-search-based and
no payload to the target firewall as the background traffic.             could be any of other algorithms described in Section III-C.
The header of dummy packets are chosen such that they will               We repeat this test 10 times and compute the median value
be discarded by the rule configured in the firewall, i.e., these         of the PPT. The median value is preferred over mean
dummy packets never pass through the firewall. The dummy                 value because it is less sensitive to outliers, which can be
packet rate varies from 870,000 packets to 1,875,000 packets             caused by the variability of network congestion and interface
per second. Since packets have no payload, the dummy traffic             packet buffering, especially when the firewalls are under load.
varies from 250Mbps to 300Mbps. Because the firewalls                    Figure 2 shows the median value of the PPT.
are installed on powerful machines, they are not under any                  Figures 2 (a) and (b) show the median value of PPT
type of resource constraints in terms of CPU and memory                  measurements with and without background traffic load for
when the firewalls are under the background traffic load. This           FW1. Using the remote measurement method, we observe
indicates that the experimental results for the firewalls under          that the median PPT increases as the rule index when there
the background traffic load may not be affected by hardware              is no background traffic load. A similar increasing trend is
resource constraints.                                                    also observed on median PPT under background traffic load
   We use two methods for measuring PPT: (1) Local mea-                  when the local measurement method is used. The slopes for the
surements are based on packet traces collected from the                  regression lines for PPT of FW1 using remote measurement
incoming and outgoing interfaces of the firewall. (2) Remote             in Figure 2(a) for with-load and no-load curves are 0.1176
measurements are based on the packet traces collected from               and 0.1645, respectively. Similarly, the corresponding slopes
the PMTA’s incoming and outgoing interfaces. The local                   for curves in Figure 2(b) are 0.1411 and -0.0317, respectively.
measurements of PPT are more accurate than the remote                    This observation implies that FW1 is likely to use a sequential-
measurements of PPT, but they require (1) local access to the            search-based packet classification algorithm. The very small
firewalls and (2) the firewall interface must support packet             negative slope of the median PPT using local measurement
analyzers which dump packets passing through the firewall’s              under no background traffic load may indicate that FW1 uses
interfaces. In contrast to local, the remote measurement of PPT          some type of rule pre-fetching or caching, yet as the slope is
includes the packet transmission time, reducing the accuracy.            very small the effect is not significant.
Because FW3 does not support any packet analyzers, we                       The results for FW2 (shown in Figures 2 (c) and (d)) suggest
use local measurement for FW1 and FW2 as well as remote                  similar sequential-search-based classification algorithms, espe-
measurement for all three firewalls to compare between FW1,              cially when the firewall is under load. The slopes for regression
FW2, and FW3.                                                            lines for PPT curves for with and without background traffic
                                                                         load in Figure 2(c) are 0.1339 and 0.0208, respectively. Sim-
                                                                         ilarly, the corresponding slopes for PPT curves in Figure 2(d)
B. Packet Classification Algorithm                                       are 0.3809 and −0.0073, respectively. We can also observe
   Identifying the exact packet classification algorithm that the        that FW1 and FW2 has considerably different transmission
firewall uses is very difficult if we treat the firewall as a black      delay especially when FW1 is under load by comparing
1704                                             IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017



the differences between graphs in remote measurement (i.e.,
Figures 2(a) and (c)) with their corresponding ones in local
measurement (i.e., Figures 2(b) and (d)). Since the experiment
environment is the same for FW1 and FW2, it seems that such
difference is due to the different queuing implementation in
FW1 and FW2, yet because we do not have access to both
firewalls source codes, it is difficult to ensure.
   We have different observations on median PPT for FW3.
The slopes for regression lines for PPT curves for with and
without background traffic load in Figure 2(e) are 0.0033 and
0.0082, respectively. The fairly flat regression lines for FW3
implies that FW3 likely uses some other techniques rather         Fig. 3. The STD of the PPT for probe packets that match against a rule in
than sequential search based algorithm for packet classifi-       the firewall policy. (a) Local measurement. (b) Remote measurement.
cation. As FW3 is a hardware firewall, we believe it uses
TCAM-based packet classification methods, which use parallel
exhaustive search.                                                that in mind, Figure 3(b) shows the STD of the firewall PPT
   2) Sensitivity to Traffic Load: Using the same experimental    calculated from the remote measurements. We observe that,
settings, we also evaluated the sensitivity of firewall perfor-   on average the STD of PPT on FW2 is 1.7910 and 33.3 times
mance to traffic load. We observe that, among all firewalls,      larger than those on FW1 and FW3, respectively. In con-
FW1 is most sensitive and FW3 is the least sensitive to the       clusion, the hardware firewall (FW3) shows less sensitivity
traffic load. Considering all remote measurements shown in        to the traffic load and seems to be more stable in terms of
Figures 2 (a), (c), and (e), the median PPT when the firewalls    performance under different network traffic loads.
are under background traffic load is 4.6034, 2.7385, and             3) Average PPT: In general, Figure 2 shows that FW3 yields
0.9874 times larger than the median PPT when the firewalls        the lowest PPT regardless of the background traffic load on
are under no background traffic load for FW1, FW2, and FW3,       the firewall. The average PPTs without background traffic
respectively.                                                     load on FW1, FW2, and FW3 are 151.7891, 77.5470, and
   We observe that the PPT curves for FW1 and FW2 have            60.3360 microseconds in the remote measurements (shown
sharper slopes when the firewalls are under the load. This        in Figures 2 (a), (c), and (e)), respectively. Similarly, the
implies that the packet classification mechanism, including       corresponding figures with traffic load on FW1, FW2, and
packet classification algorithm and possible caching scheme,      FW3 are 672.8522, 98.7970, and 57.8777 microseconds,
depends on the current traffic and load on the firewall.          respectively. However, in the local measurements (shown in
   We also find that the traffic load on the firewall has an      Figures 2(b) and (d)), FW1 and FW2 have similar average
impact on the variance and dispersion of the PPT of probe         PPTs (50.3710 and 49.7796 microseconds, respectively) when
packets, which directly relate to the stability of the firewall   there is no background traffic load on the firewalls. On the
and firewall packet reordering. Figure 3 shows the standard       other hand, when there is background traffic load on the
deviation (STD) of the PPT for probe packets. We observe          firewall, the average PPTs for FW1 and FW2 are 126.7352 and
in Figure 3(a) that on average the STD of the PPT for FW2 has     92.8078 microseconds, respectively. This result again indicates
52.4749 times larger than that for FW1 in local measurements.     the high sensitivity of FW1 to the traffic load. Overall, FW3
   To show the relation between the STD of the PPT of probe       outperforms FW1 and FW2 with the lowest average and
packets in local and remote measurement, let SiL and SiR          minimum STD of PPTs, and the least sensitivity to the traffic
denote the vector of PPTs obtained in local and remote            load.
measurements for the i -th firewall, respectively. Let T denote
the transmission delay from PMTA to the firewall and from
the firewall to PMTA. Therefore, SiR = SiL + T . The STD          C. Caching and Statefulness
of the PPT for local measurement can be calculated from the          As explained, modern firewalls often use different caching
STD of the PPT for remote measurement as follows:                 mechanisms for rule and flow caching to reduce the per-
                                                                  formance overhead of packet classification. To identify if
   ST D(SiR ) = ST D(SiL + T )
                !                                                 a firewall uses caching and how effective the caching is,
              = ST D(SiL )2 + ST D(T )2 + C OV (SiL , T )         we define cache effectiveness (C) as the ratio of the PPT for
                                                                  the first probe packet in a sequence of probe packets, whose
  As PPT and transmission time are independent,                   header is not in the cache table, to the median PPT of the rest
C OV (SiL , T ) = 0. Also, STD of transmission time can           of the probe packets in the same sequence, whose headers are
be represented by a constant vector c . Hence,                    supposedly in the cache. If C > 1, the firewall uses caching
                           !
                                                                  effectively. If C ≃ 1, the firewall either does not use caching,
               ST D(SiL ) = ST D(SiR )2 − c2
                                                                  or the caching that the firewall uses is not effective.
Note that the transmission time and its standard deviation           In our experiments, we measure the firewall’s C value as
can be different based on the load on the firewalls and the       follows. For each experiment we first send 10 probe packets
way the firewalls handle queuing and packet forwarding. With      and measure the PPT for each probe packet and calculate
LIU et al.: FIREWALL FINGERPRINTING AND DoF ATTACKS                                                                              1705



                                                              TABLE II
                                  C ACHE E FFECTIVENESS BASED ON L OCAL AND R EMOTE M EASUREMENTS




C value accordingly. The C value reported in this paper is the         In the remote measurements, the transmission delay is quite
median C values for 10 repeated experiments. To determine           larger than the actual PPT. Therefore, the cache effectiveness
the effectiveness of a firewall caching in stateless and stateful   calculated based on remote measurement are not as expressive.
modes, we conduct experiments using four types of probe             However, there is one exception where FW3 has a cache
packet modes: TCP Fix, TCP Vary, UDP Fix, and UDP Vary.             effectiveness of larger than 2 in UDP Fix probe packets when
If the firewall has effective caching in TCP Fix and UDP Fix        FW3 is in the stateful mode. The result indicates that FW3
probe packets, it means that the firewall caches all 5 packet       performs flow caching in this typical case. In addition, we
header fields in its cache table. i.e., it performs flow caching.   find a unique feature on FW3. When FW3 is in stateful mode,
However, if a firewall has effective caching in TCP Vary and        once PMTA sends the first TCP SYN packet, FW3 does not
UDP vary probe packets, it means the firewall caches only           accept any other TCP packet with the same packet header
4 packet header fields (excluding source port) in its cache         for a while until it receives a TCP ACK packet from the
table. i.e., it performs rule caching.                              packet destination. Thus, calculating C for TCP Fix for FW3
   Table II presents the C values calculated based on local         in stateful mode is not applicable.
and remote measurements for experiments on three firewalls             Another observation that we can make from the results is
FW1, FW2 and FW3. In the local measurements, the results            that in most of the cases, the cache effectivenesses when a
show that FW1 performs very effective flow caching on               firewall does not have background traffic load are slightly
UDP packets as the cache effectivenesses for UDP Fix are            higher than those when a firewall has background traffic load.
significantly more than 1 (7.4931 < C < 16.75). However,            One possible explanation is that when a firewall is under load,
FW1 flow caching on TCP packets is not as effective since           the cache table has a large number of entries. This results
the cache effectivenesses for TCP Fix are variable around 2         in longer search time (and PPT) for the rest of the probe
(1.9038 < C < 2.3411). In addition, the results imply that          packets. This makes the cache less effective compared to
FW1 does not support rule caching because the cache effec-          no-load experiments, where the firewall’s cache table has a
tivenesses for UDP Vary and TCP Vary are around 1. However,         small number of entries.
there is one exception case where the cache effectiveness for
TCP Vary probe packets when FW1 is under the background             D. Impact of Packet Protocol and Payload Size
traffic load and configured in stateless mode is 3.2020. This          Firewalls usually perform queuing management techniques
could be an indication of some caching mechanisms that are          to improve their PPT. Such techniques can be made to be
enabled when FW1 is under load.                                     aware of the protocol and payload size of packets. In order
   For FW2, the caching effectivenesses for UDP Fix and TCP         to evaluate the impact of packet protocol and payload size,
Fix experiments range from 5.4214 to 9.8167, while the cor-         we configure all three firewalls in the stateless mode and repeat
responding figures for UDP Vary and TCP Vary experiments            the same set of experiments while varying the packet payload
range from 2.8588 to 4.6833. Because the cache effectiveness        size. Figure 4 shows the median PPT results for packet payload
values for FW2 are much larger than 1 for all experiments,          size of 0, 500, 1000, 1400 bytes.
it seems that FW2 uses rule caching. In addition, because the          We have three main observations from the results. First,
cache effectivenesses for UDP Fix and TCP Fix experiments           Figures 4(a), (b), (d) and (e) show that software firewalls
are larger than those for UDP Vary and TCP Vary experi-             FW1 and FW2 have different PPT in TCP Fix, TCP Vary,
ments, seemingly the FW2 uses separate flow caching and             UDP Fix, and UDP Vary experiments. We observe that the
rule caching mechanisms. Comparing the cache effectiveness          PPTs are smaller in TCP Fix and UDP Fix probe packets
results for FW1 and FW2, the flow caching mechanism on              than those in TCP Vary and UDP Vary probe packets. This
FW1 is more effective on UDP packets, whereas the flow              can be a result of effective flow caching on FW1 and FW2.
caching mechanism on FW2 is more effective on TCP flows.            More specifically, this observation on FW2 seems to indicate
1706                                                      IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017




Fig. 4. The remote PPT for probe packets with different packet payload sizes. (a) FW1 with load. (b) FW2 with load. (c) FW3 with load. (d) FW1 with no
load. (e) FW2 with no load. (f) FW3 with no load.



that flow caching is more effective than rule caching on FW2.               in the typical firewall settings. However, in some uncommon
Note that the above observations are made regardless of the                 cases, certain types of firewalls support policies that are
packet payload size. However, Figures 4(c) and (f) shows that               based on TCP flags in addition to the other common TCP
FW3 has the same PPT for all of the TCP Fix, TCP Vary,                      header information. This makes our binary sequence decision
UDP Fix, and UDP Vary packets. This indicates that if FW3                   unreliable. Hence, we further supplement the decision binary
has the same rule caching mechanism for all TCP and UDP                     sequence with the PPT of the probe packets and use them
packets (when it is in stateless mode). Second, the results in              to infer the target firewall implementation. Note that in the
Figures 4(a), (b), (d) and (e) indicate that the packet payload             remainder of the paper, all PPTs are measured remotely.
size does not impact the PPT on FW1 and FW2. However,                          To extract the firewall behavior fingerprints and construct
Figures 4 (c) and (f) show that the PPT increases linearly with             classifiers, we first establish a controlled environment, which
regression slope of 0.1945 ±0.0014 as the packet payload size               includes various candidate firewalls of interest, devices outside
increases. Finally, we observe from Figure 4 that the impact                the firewall that can be used to launch probe packets and
of packet protocol and payload size on PPT is independent                   devices behind the firewall that can be used to receive the
from whether the firewall has background traffic load.                      probe packets and measure the delays. The testbed network
                                                                            shown in Figure 1 is an example of such a set up. The
                   VI. F IREWALL I NFERENCE                                 signatures and classifiers identified herein can then be applied
   The first step toward defeating an opponent is to know                   in the “battlefield”. We next describe in detail the methodology
them – the same principle applies when it comes to attacking                we applied for firewall implementation inference and present
a firewall. If attackers can successfully infer the type (e.g.,,            the result for the three firewalls in our testbed. While our
vendor/version) and the characteristics (e.g.,, statefulness) of            testbed is limited to the three different firewalls available to us,
the target firewalls, they can potentially render a much more               we believe that our methodology is more generally applicable
effective attack. In this section, we examine the feasibility and           for fingerprinting other firewalls in the market.
effectiveness of firewall implementation inference using probe
packets.                                                                    A. Firewall Inference Using TCP Probe Packets
   Our approach is motivated by the wide range of so-called                    As there are eight different TCP flags defined in a TCP
operating system (OS) fingerprinting [29]–[31] techniques.                  header, one can construct 28 different combinations in each
The idea is that different OSes respond to non-conforming pro-              probe packet. This can be further compounded by the per-
tocol (such as TCP, HTTP) interactions differently. By tracking             mutations of different probe packet sequences. In our limited
the error-handling responses, one may uniquely identify the                 testbed case, we find that it is sufficient to use two consecutive
OS of the target host. In our case, we study the decision of                probe packets to distinguish the behavior of different firewalls
firewalls for sequences of TCP probe packets with varying                   (and different modes – stateful and stateless). We show the
TCP header flags – the decision of the firewall is limited                  results when we enable the TCP SYN flag along with one other
to a binary sequence of whether the corresponding packet                    TCP flag in each of the two packets. Table III and Table IV
is accepted or discarded by the firewall. To distinguish the                present the results for the stateful and the stateless firewalls,
firewall’s accept/discard decision due to its configured policy,            respectively. For the ease of presentation, we condense the
we force all probe packets in the same sequence to share                    information in the table such that the columns represent the
the same source IP, destination IP and destination port. This               different TCP flags enabled in the first probe packet (besides
ensures that these probe packets hit the same firewall rule                 the TCP SYN flag) and the rows represent the different TCP
LIU et al.: FIREWALL FINGERPRINTING AND DoF ATTACKS                                                                                  1707



                            TABLE III                                and without payload (total of 8 times). Each data point is
                       S TATEFUL F IREWALL                           represented by x = ⟨x 1 , · · · , x 24 ⟩ that has 24 features where
                                                                     x 3i−2 is the median of the PPTs, x 3i−1 is the STD of the
                                                                     PPTs, and x 3i is the cache effectiveness for the 11 probe
                                                                     packets. The data points are collected when the firewalls are
                                                                     under three different load levels: no load, medium load, and
                                                                     full load. We also use three set of labels: the labels for
                                                                     the firewall type (Y1 = {‘FW1’,‘FW2’,‘FW3’}), the labels
                                                                     for the firewall statefulness (Y2 = {’stateful’, ’stateless’}), and
                                                                     the labels for firewall type and statefulness (Y3 = {‘FW1-SF’,
                            TABLE IV                                 ‘FW2-SF’,‘FW3-SF’,‘FW1-SL’,‘FW2-SL’,‘FW3-SL’}).
                      S TATELESS F IREWALLS                             For the classification, we use multi-class Support Vector
                                                                     Machines (SVMs) with Radial Basis Function (RBF) kernel
                                                                     with parameters, (γ = 0.01, C=500). Note that the value for
                                                                     RBF kernel parameters have been chosen by model selec-
                                                                     tion algorithms that we used to maximize the classification
                                                                     accuracy. We conduct classification separately for each set
                                                                     of labels, under two conditions (1) if the attacker somehow
                                                                     knows the firewall load and (2) if the attacker does not know
                                                                     the firewall load when he tries to infer firewall implementation
                                                                     and its statefulness.
flags enabled in the second probe packet (besides SYN flag).            The classification problem under first condition is a classic
In each cell, there are three indicators representing the firewall
                                                                     one, where the data point is x with an additional feature of
decision from the firewall FW1, FW2 and FW3 respectively.
                                                                     firewall load level, denoted by z. However, the classification
Indicator “*” means that both probe packets are accepted by
                                                                     problem under the second condition is not as straightforward.
the firewall and successfully received at the receiving device
                                                                     When we train the classifier we know the firewall load level,
behind the firewall; indicator “-” means that one or both probe      but when we use it for testing we do not know the load
packets are discarded, or more strictly speaking, missed by the
                                                                     level. To solve this problem, we first formulize the problem
receiver.
                                                                     as follows:
   The result in Table III and IV demonstrates that tracking                                     "
the firewalls’ feedback to well-crafted TCP probe packets can                         P(Y |x) =        P(Y, z|x)                      (1)
be effective in obtaining valuable information to distinguish                                     "
                                                                                                   z
different firewalls. Unlike FW1 and FW3 that filter out some                                  =        P(Y |z, x)P(z|x)              (2)
probe packets, for the stateful and stateless setting of FW2,                                      z
both probe packets have passed through the firewall in all              Formula 2 indicates that we need two probabilistic classi-
98 cases. While our example uses certain combinations of TCP
                                                                     fiers: the first one is to speculate the firewall load level (z)
flags, other combinations can prove useful for other firewall
                                                                     given test data point (x), and the second classifier is to predict
types and settings. We next demonstrate that one can use             the firewall label, given the test data point and the speculated
supplement information from the PPTs to distinguish these
                                                                     load level. Using the probabilistic classifiers, we first calculate
cases.
                                                                     the probability of each label for a given data point and then
                                                                     choose the label with the highest probability as the final
B. Firewall Inference Using Packet Processing Time                   label of the data point. For the probabilistic classifier, we use
   Although firewall fingerprinting using a sequence of TCP          multi-class libsvm with probability estimates [32].
packets is a deterministic method used to infer firewall imple-         Before classification, we use feature selection to maximize
mentation, firewall rules on TCP flags can change the default        classification accuracy rates. By using feature selection, we
decision of the firewall and cause misidentification of the fire-    not only alleviate the curse of dimensionality, but also find the
wall implementation. Thus, we propose an alternate method to         most important and distinctive features in our feature set. Thus,
use implementation characteristics including PPT and firewall        we use Sequential Forward Searching (SFS) algorithm [33] for
cache effectiveness to infer firewall implementation and its         feature selection for each set of labels. The results indicate
statefulness. In this method, we build a classification model        that: (1) To infer the firewall implementation, we only need
for each firewall and for their statefulness modes based on          6 features that contain median of PPTs for probe packets
their median PPT, STD of the PPTs, and cache effectiveness.          in TCP Fix and TCP Vary modes with payload, and cache
We then use this classification model to infer firewall imple-       effectiveness for all probe modes with payload. (2) To infer the
mentation.                                                           statefulness, we need 16 features that contain all probe packets
   To build a classification model and analyze its accuracy, we      with payload features as well as cache effectiveness of probe
first create a dataset containing 3,600 data points. For each data   packets with no payload features. (3) To infer the firewall
point, we send 11 consecutive probe packets in four different        implementation and statefulness, we need 7 features that
modes (TCP Fix, TCP Vary, UDP fix and UDP vary) with                 contain the cache effectiveness for all probe test modes with
1708                                               IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017



                           TABLE V                                    The results in Table VII show that we can predict the type
  A CCURACY AND M ISCLASSIFICATION FOR F IREWALL T YPE L ABELS     of a firewall and its statefulness with 74.04% and 74.06%
                                                                   of accuracy for known load and unknown load, respectively,
                                                                   which is relatively good as we have six classes and random
                                                                   classification accuracy rate is 16.67%. As show in Table V,
                                                                   FW1 is classified with high accuracy, while FW2 is classified
                                                                   with relatively low accuracy. Inferring FW3, on the other hand,
                                                                   can be done with very good accuracy. The misclassification
                                                                   rates also suggest that both stateful FW1 and FW2 are mis-
                                                                   classified as stateless FW1 and FW2 with high probability
                                                                   of 30.61%.
                                                                      If we use a different set of firewalls we may have different
                                                                   results for accuracy and misclassification rates. However, the
                                                                   results for this set of firewalls indicate that an attacker can
                          TABLE VI                                 effectively use these two methods to predict a network firewall
   A CCURACY AND M ISCLASSIFICATION FOR S TATEFULNESS L ABELS      and design attacks accordingly to either seriously impact
                                                                   performance or exploit possible vulnerabilities. Nevertheless,
                                                                   in practice, the accuracy results will be lower because of the
                                                                   impact of the transmission delay induced by other middleboxes
                                                                   along the probe path. Yet, the attacker can obtain the number of
                                                                   routers in the probe path (using tools such as traceroute)
                                                                   and build a similar testbed for learning to reduce such impact.

                                                                             VII. D ENIAL OF F IREWALLING ATTACKS
                                                                      In order to effectively attack firewalls, we first use firewall
                                                                   characteristic measurements (conducted earlier in section V)
payload, median of PPTs for probe packets in TCP Fix and           to design effective customized attacks on the firewall. We then
UDP Vary modes with payload, and cache effectiveness for           examine the effectiveness of the customized attacks by com-
probe packets in UDP Vary mode with no payload. Note that          paring the firewall performance under the customized attacks
when using different methods of classification, the important      with the firewall performance under blind attacks.
features may be different. However, our results show that             The experimental methodology herein is to create an attack
the most distinctive features are the cache effectiveness for      scenario and monitor the firewall performance on legitimate
the probe packets with payload which clearly complies with         traffic. In our testbed setup, we drive attacks from all machines
Figure 4.                                                          in VM1 and VM2 and send (legitimate) probe packets from
   The accuracy results for the classification under               the PMTA machine. We use the PPT observed by the probe
two conditions for each set of labels are reported                 as the performance indicator metric. We also use the CPU
in Tables V, VI, and VII. The results are the mean of              utilization on the firewall device as a measure of the firewall
10 cross-validation accuracy and misclassification results for     “stress” level. In our testbed, this CPU utilization information
the dataset.                                                       is available from FW1 and FW2, obtained through Simple Net-
   The results in Table V indicate that we can predict the         work Management Protocol (SNMP). FW3 does not provide
firewall implementation with 94.56% and 94.61% accuracy            access to this information.
for known load and unknown load, respectively. The results            In a blind attack, VM1 and VM2 send random UDP and
also show that while the accuracy of predicting FW1 is 100%,       TCP packets with no payload, which are mostly discarded
the accuracy of predicting FW2 is 84.36% (and 84.58% for           by the firewall. In contrast, the customized attack pack-
unknown load) because in 12.13% (and 12.41% for unknown            ets are chosen to be accepted by the firewall. The attack
load) of the time it is misclassified by FW1. The closeness of     packets are generated in TCP Vary and UDP Vary modes.
the accuracy rates for known load and unknown load assump-         Figure 5 and Figure 6 show the packet processing time for
tions shows that the firewall load level plays an insignificant    500 probe packets sent with one second interval when the
role in classification. Note that if we use other classification   firewalls are under blind attack, UDP Vary attack and TCP
methods or other set of firewalls, the firewall load level can     Vary attack. Note that in Figure 5 the customized attack
be an important factor in classification. Thus, it should not be   packets have no payload, while in Figure 6 they have packet
overlooked.                                                        payload.
   The results in Table VI show that we can predict the state-        Figure 5(a) and 5(d) show the probe PPTs for FW1 when
fulness of the firewall with 85.79% and 85.72% of accuracy         it is in the stateless and stateful modes, respectively. The
for known load and unknown load, respectively. Surprisingly,       results indicate that the TCP and UDP Vary attacks are more
the accuracy rate is very good knowing that the statefulness       successful than blind attack, whereas UDP Vary and TCP Vary
of a firewall has a trivial impact on the PPT.                     almost have the same effect on FW1. The average PPT for
LIU et al.: FIREWALL FINGERPRINTING AND DoF ATTACKS                                                                                            1709



                                                                   TABLE VII
                              A CCURACY AND M ISCLASSIFICATION FOR F IREWALL T YPE AND S TATEFULNESS L ABELS




Fig. 5. The PPT of probe packets when the firewall is under attack with packet with no payload. (a) FW1 - Stateless. (b) FW2 - Stateless. (c) FW3 -
Stateless. (d) FW1 - Stateful. (e) FW2 - Stateful. (f) FW3 - Stateful.


probe packets when FW1 is in stateless mode under UDP and                     Figure 5(b) and 5(e) show the probes PPT for FW2 when
TCP Vary attacks are 686.10 and 673.86 microseconds, which                 it is in the stateless and stateful modes, respectively. First,
are 1.68 and 1.65 times larger than when it is under blind                 the results indicate that the average PPT of probe packets
attack. However, TCP and UDP Vary attacks impose up to                     when the firewall is under blind attack is 1.82 times more than
2.08 and 2.06 times more load than the blind attack, meaning               when the firewall is under UDP attack. Second, the average
that the firewall needs more than twice the processing power               PPT of probe packets when the firewall is under TCP Vary
to handle the attack. The average PPT results are almost the               attack is 22.4 and 25.77 times more than when the firewall
same when the firewall is in the stateful mode. However, the               is under blind attack for the stateless and stateful modes,
firewall load has 21.36% increase when it is in the stateful               respectively. Moreover, when the firewall is under TCP Vary
mode. Hence, if the attacker knows an accepted rule, the attack            attack 36.20% and 34.40% of the probe packets are discarded
on that rule will be much more effective. Note that for firewalls          before reaching the destination (i.e., packet loss) when the
like FW1 which use sequential search for packet processing, if             firewall is in the stateless and stateful modes, respectively.
an attacker finds a rule with a higher rule index in the firewall          However, looking at the average load of the firewall, there
policy, it can increase the effectiveness of the attack. However,          is only 0.14 increase for TCP Vary attack compared to a
this depends highly on the size of the firewall policy and the             blind attack. This can somehow imply that the caching table
firewall caching algorithm [12].                                           is flooded by new flows, which reduces the performance
1710                                                        IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017




Fig. 6. The PPT of probe packets when the firewall is under attack with packets with payload. (a) FW1 - Stateless. (b) FW2 - Stateless. (c) FW3 - Stateless.
(d) FW1 - Stateful. (e) FW2 - Stateful. (f) FW3 - Stateful.



of the system tremendously, while the system has enough                        for the TCP Vary tests in all the firewall modes. Similarly,
resources to forward attack packets with small PPT. Thus,                      Figure 6(c) and 6(f) show that the packet payload on attack
amongst all of the attacks, the TCP Vary attack is quite                       traffic does not have any impact on average PPT of probe
effective on FW2 and FW2 is quite resilient against UDP Vary                   packets sent to the FW3. However, the STD of PPT of
attack.                                                                        probe packets is decreased by 1.88 and 1.72 times when
   Figure 5(c) and 5(f) show the probes PPT for FW3 when it                    the firewall is in the stateless mode for TCP and UDP Vary
is in the stateless and stateful modes, respectively. The results              attack, respectively. Similarly, when FW3 is in the stateful
indicate that none of attacks has considerable impact on the                   mode, the STD of PPT of probe packets is decreased by
firewall. More specifically, when the firewall is in the stateless             2.17 and 2.67 times when the firewall is in the stateless
mode, the average PPT is 66, 67 and 67 microseconds for                        mode for TCP and UDP Vary attack, respectively. Therefore,
blind, UDP and TCP Vary attacks. When the firewall is in the                   to attack these firewalls effectively, packets with no payload
stateful mode, the average PPT increases by 6 times to 401,                    are preferred. On the other hand, this implies that the main
397 and 397 microseconds. Unlike the other two firewalls it                    focus of an administrator who is working to detect and
seems that the generated attacks on FW3 do not have much                       stop such attacks should be on attack packets that have no
impact on the firewall performance. It is also notable that                    payload as they can have an impact on the stability of the
although it seems that FW3 have the least fluctuation around                   firewall.
the mean value, there are some spikes for TCP and UDP Vary
attacks whose value is 9 times larger than the mean value
                                                                                           VIII. C ONCLUSION AND F UTURE W ORK
when firewall is in the stateless mode. Such spikes can be
seen on Figure 5(e) for stateful results as well as spikes for                    In this paper, we present methods for finding the firewall
blind attacks. The standard deviation of PPT for blind attacks                 characteristics that are introduced by firewall implementations.
when FW3 is in the stateful mode is 11.5 times more than                       Such characteristics can be exploited by attackers to identify
when FW3 is in the stateless mode.                                             black box firewalls with high accuracy and launch effective
   Figure 6 shows the impact of attack traffic on probe packets                attacks on firewalls. We show two methods for inferring
when attack traffic has payload. In this figure, the UDP and                   firewall implementation using these characteristics. The first
TCP Vary with payload PPT results for each probe packet                        method is based on the firewall decision on a sequence of
are calculated as the mean of the PPT for the probe packet                     TCP packets with unusual flags, which could be used as a
with three different payload sizes: 500B, 1000B, and 1400B.                    firewall fingerprint for identification. The second method is
Figure 6(a) and 6(d) show the packet payload size on                           based on machine learning techniques. We further study the
attack traffic have almost no effect on FW1. However,                          impact of different attacks on different firewalls and show
Figure 6(b) and 6(e) show the packet payload on attack traffic                 that different firewalls are vulnerable to different attacks.
increases the STD of PPT on probe packets sent to FW2 by                       While the best defense would involve working with firewall
2.38 times for the UDP Vary test when the firewall is in the                   manufacturers to improve firewall implementations to mini-
stateful mode and decreases the STD of PPT by 1.61 times                       mize the impact of attacks, as future work we are willing
LIU et al.: FIREWALL FINGERPRINTING AND DoF ATTACKS                                                                                                       1711



to propose defense mechanisms from the firewall adminis-                       [21] P. Gupta and N. McKeown, “Packet classification using hierarchical
trators’ perspective, particularly in preventing attackers from                     intelligent cuttings,” in Proc. Hot Interconnects VII, Aug. 1999, pp. 1–9.
                                                                               [22] S. Singh, F. Baboescu, G. Varghese, and J. Wang, “Packet classifica-
gaining information about the firewall deployed and hence                           tion using multidimensional cutting,” in Proc. ACM SIGCOMM, 2003,
forcing attackers to use less-effective, blind attacks. Such                        pp. 213–224.
mechanisms are designed to increase the chance of incorrect                    [23] P. Gupta and N. McKeown, “Packet classification on multiple fields,” in
                                                                                    Proc. ACM SIGCOMM, 1999, pp. 147–160.
firewall implementation inference by concealing firewall TCP                   [24] F. Baboescu and G. Varghese, “Scalable packet classification,” in Proc.
flag fingerprints and obscuring the pattern in probe PPT.                           ACM SIGCOMM, 2001, pp. 199–210.
To evaluate the effectiveness of these defense mechanisms                      [25] V. Srinivasan, S. Suri, and G. Varghese, “Packet classification using tuple
                                                                                    space search,” in Proc. ACM SIGCOMM, 1999, pp. 135–146.
and measure their impact on firewall performance, we need                      [26] Mz-Traffic Generator, accessed on Jan. 2017. [Online]. Available:
to conduct extensive experiments, for which we will need to                         http://www.perihel.at/sec/mz
expand our testbed. The techniques proposed in this paper                      [27] Hping, accessed on Jan. 2017. [Online]. Available: http://www.hping.org
can be potentially useful for many other applications as                       [28] TCPDUMP Public Repository, accessed on Jan. 2017. [Online].
                                                                                    Available: http://www.tcpdump.org
well [34]–[69].                                                                [29] D. Watson, M. Smart, G. R. Malan, and F. Jahanian, “Protocol scrubbing:
                                                                                    Network security through transparent flow modification,” IEEE/ACM
                                                                                    Trans. Netw., vol. 12, no. 2, pp. 261–273, Apr. 2004.
                                                                               [30] L. G. Greenwald and T. J. Thomas, “Toward undetected operating system
                             R EFERENCES                                            fingerprinting,” in Proc. USENIX Workshop Offensive Technol., 2007,
                                                                                    Art. no. 6.
 [1] D. Goodin. (2010). Hacker Pierces Hardware Firewalls With                 [31] R. Beverly, “A robust classifier for passive TCP/IP fingerprinting,” in
     Web Page. [Online]. Available: http://www.theregister.co.uk/2010/01/06/        Proc. PAM, 2004, pp. 158–167.
     web_based_firewall_attack/                                                [32] T.-F. Wu, C.-J. Lin, and R. C. Weng, “Probability estimates for multi-
 [2] (2011). Cisco Firewall Services Module DoS Vulnerability. [Online].            class classification by pairwise coupling,” J. Mach. Learn. Res., vol. 5,
     Available: http://www.net-security.org/secworld.php?id=10673                   pp. 975–1005, Dec. 2004.
 [3] Z. Qian and Z. M. Mao, “Off-path TCP sequence number inference            [33] P. Somol, P. Pudil, J. Novovičová, and P. Paclík, “Adaptive floating
     attack—How firewall middleboxes reduce security,” in Proc. IEEE Symp.          search methods in feature selection,” Pattern Recognit. Lett., vol. 20,
     Secur. Privacy, Oakland, CA, USA, May 2012, pp. 347–361.                       nos. 11–13, pp. 1157–1163, Nov. 1999.
 [4] Fyodor. Nmap: Free Network Security Scanner, accessed on Jan. 2017.       [34] Z. Fu, F. Huang, X. Sun, A. Vasilakos, and C.-N. Yang, “Enabling
     [Online]. Available: http://nmap.org                                           semantic search based on conceptual graphs over encrypted outsourced
 [5] F. V. Yarochkin, O. Arkin, M. Kydyraliev, S.-Y. Dai, Y. Huang, and             data,” IEEE Trans. Serv. Comput., to be published.
     S.-Y. Kuo, “Xprobe2++: Low volume remote network information              [35] Z. Fu, K. Ren, J. Shu, X. Sun, and F. Huang, “Enabling personalized
     gathering tool,” in Proc. DSN, Jun./Jul. 2009, pp. 205–210.                    search over encrypted outsourced data with efficiency improvement,”
 [6] M. Zalewski. P0f, accessed on Jan. 2017. [Online]. Available:                  IEEE Trans. Parallel Distrib. Syst., vol. 27, no. 9, pp. 2546–2559,
     http://lcamtuf.coredump.cx/p0f.shtml                                           Sep. 2016.
 [7] J. P. S. Medeiros, A. M. Brito, and P. S. M. Pires, “A new method for     [36] Z. Xia, X. Wang, L. Zhang, Z. Qin, X. Sun, and K. Ren, “A privacy-
     recognizing operating systems of automation devices,” in Proc. IEEE            preserving and copy-deterrence content-based image retrieval scheme in
     EFTA, Sep. 2009, pp. 1–4.                                                      cloud computing,” IEEE Trans. Inf. Forensics Security, vol. 11, no. 11,
                                                                                    pp. 2594–2608, Nov. 2016.
 [8] Snacktime: A Perl Solution for Remote OS Fingerprinting,
     accessed on Jan. 2017. [Online]. Available: http://www.planb-             [37] Z. Pan, J. Lei, Y. Zhang, X. Sun, and S. Kwong, “Fast motion
     security.net/wp/snacktime.html                                                 estimation based on content property for low-complexity H.265/HEVC
                                                                                    encoder,” IEEE Trans. Broadcast., vol. 62, no. 3, pp. 675–684,
 [9] M. Smart, G. R. Malan, and F. Jahanian, “Defeating TCP/IP stack
                                                                                    Sep. 2016.
     fingerprinting,” in Proc. USENIX Secur., 2000, pp. 229–240.
                                                                               [38] B. Gu, V. S. Sheng, K. Y. Tay, W. Romano, and S. Li, “Incremental
[10] E. Millard. (2003). Best Firewalls for Big Enterprises. [Online].
                                                                                    support vector learning for ordinal regression,” IEEE Trans. Neural
     Available: http://www.ecommercetimes.com/story/business/21764.html
                                                                                    Netw. Learn. Syst., vol. 26, no. 7, pp. 1403–1416, Jul. 2015.
[11] M. Shahzad and A. X. Liu, “Noise can help: Accurate and efficient per-    [39] J. Li, X. Li, B. Yang, and X. Sun, “Segmentation-based image copy-
     flow latency measurement without packet probing and time stamping,”            move forgery detection scheme,” IEEE Trans. Inf. Forensics Security,
     in Proc. ACM Int. Conf. Meas. Modeling Comput. Syst. (SIGMETRICS),             vol. 10, no. 3, pp. 507–518, Mar. 2015.
     Austin, TX, USA, Jun. 2014, pp. 207–219.
                                                                               [40] X. Wen, L. Shao, Y. Xue, and W. Fang, “A rapid learning algorithm for
[12] K. Salah, K. Sattar, M. Sqalli, and E. Al-Shaer, “A potential low-rate         vehicle classification,” Inf. Sci., vol. 295, pp. 395–406, Feb. 2015.
     DoS attack against network firewalls,” J. Secur. Commun. Netw., vol. 4,   [41] Z. Fu, X. Sun, S. Ji, and G. Xie, “Towards efficient content-aware search
     no. 2, pp. 136–146, Feb. 2011.                                                 over encrypted outsourced data in cloud,” in Proc. IEEE INFOCOM,
[13] M. R. Lyu and L. K. Y. Lau, “Firewall security: Policies, test-                Apr. 2016, pp. 1–9.
     ing and performance evaluation,” in Proc. Int. Conf. Comput. Syst.        [42] Z. Zhou, Y. Wang, Q. M. J. Wu, C.-N. Yang, and X. Sun, “Effec-
     Appl. (COMPSAC), Oct. 2000, pp. 116–121.                                       tive and efficient global context verification for image copy detec-
[14] R. Funke, A. Grote, and H.-U. Heiss, “Performance evaluation of                tion,” IEEE Trans. Inf. Forensics Security, vol. 12, no. 1, pp. 48–63,
     firewalls in gigabit-networks,” in Proc. Symp. Perform. Eval. Comput.          Jan. 2017.
     Telecommun. Syst., 1999, pp. 1–5.                                         [43] B. Gu, X. Sun, and V. S. Sheng, “Structural minimax probability
[15] B. Bosen. (2007). Firewall Performance Evaluation Secure                       machine,” IEEE Trans. Neural Netw. Learn. Syst., to be published.
     Computing Sidewinder vs. Check Point NGX. [Online]. Available:            [44] B. Gu and V. S. Sheng, “A robust regularization path algorithm for
     http://www.comguard.cz/fileadmin/user_upload/aktuality/SIDE-vsCheck            v-support vector classification,” IEEE Trans. Neural Netw. Learn. Syst.,
     pointPerformanceEval.pdf                                                       to be published.
[16] The Tolly Group. (2002). Competitive Evaluation of Enterprise-Class       [45] Z. Pan, Y. Zhang, and S. Kwong, “Efficient motion and dis-
     Internet Security Solutions. [Online]. Available: http://www.tolly.            parity estimation optimization for low complexity multiview video
     com/TS/2002/CheckPoint/VPN-1Pro/TollyTS202132CheckPointVPN-                    coding,” IEEE Trans. Broadcast., vol. 61, no. 2, pp. 166–176,
     1ProAug02-b.pdf                                                                Jun. 2015.
[17] (2009). Sidewinder G2 Startup Guide. [Online]. Available:                 [46] Z. Xia, X. Wang, X. Sun, and Q. Wang, “A secure and dynamic multi-
     http://www.securecomputing.com/techpubs_download.cfm?id=1642                   keyword ranked search scheme over encrypted cloud data,” IEEE Trans.
[18] CheckPoint FireWall-1, accessed on Jan. 2017. [Online]. Available:             Parallel Distrib. Syst., vol. 27, no. 2, pp. 340–352, Jan. 2016.
     http://www.checkpoint.com/                                                [47] Z. Fu, X. Wu, C. Guan, X. Sun, and K. Ren, “Toward efficient multi-
[19] (2011). Cisco PIX Firewalls. [Online]. Available: http://www.cisco.com/        keyword fuzzy search over encrypted outsourced data with accuracy
[20] (2008). NetScreen-500. [Online]. Available: http://www.juniper.                improvement,” IEEE Trans. Inf. Forensics Security, vol. 11, no. 12,
     net/us/en/products-services/end-of-sale/ns500/                                 pp. 2706–2716, Dec. 2016.
1712                                                          IEEE TRANSACTIONS ON INFORMATION FORENSICS AND SECURITY, VOL. 12, NO. 7, JULY 2017



[48] Y. Cai, F. R. Yu, C. Liang, B. Sun, and Q. Yan, “Software-defined                                  Alex X. Liu received the Ph.D. degree in computer
     device-to-device (D2D) communications in virtual wireless networks                                 science from The University of Texas at Austin
     with imperfect network state information (NSI),” IEEE Trans. Veh.                                  in 2006. His research interests focus on network-
     Technol., vol. 65, no. 9, pp. 7349–7360, Sep. 2015.                                                ing and security. He received the IEEE & IFIP
[49] F. Chen, K.-W. Wong, X. Liao, and T. Xiang, “Period distribution of                                William C. Carter Award in 2004, the National Sci-
     generalized discrete Arnold cat map,” Theor. Comput. Sci., vol. 552,                               ence Foundation CAREER Award in 2009, and the
     no. 5, pp. 13–25, Oct. 2014.                                                                       Michigan State University Withrow Distinguished
                                                                                                        Scholar Award in 2011. He is an Associate Editor
[50] J. Chen, Y. Wang, and X. Wang, “On-demand security architecture for                                of IEEE/ACM T RANSACTIONS ON N ETWORKING
     cloud computing,” Computer, vol. 45, no. 7, pp. 73–78, Jul. 2012.                                  and IEEE T RANSACTIONS ON D EPENDABLE AND
[51] J. Chen, G. Wu, and Z. Ji, “Secure interoperation of identity manage-                              S ECURE C OMPUTING, and an Area Editor of Com-
     ments among different circles of trust,” Comput. Standards Interfaces,       puter Communications. He received Best Paper Awards from ICNP-2012,
     vol. 33, no. 6, pp. 533–540, Nov. 2011.                                      SRDS-2012, and LISA-2010.
[52] J. Chen, G. Wu, L. Shen, and Z. Ji, “Differentiated security levels
     for personal identifiable information in identity management system,”
     Expert Syst. Appl., vol. 38, no. 11, pp. 14156–14162, 2011.                                          Amir R. Khakpour received the B.S. degree in
                                                                                                          electrical engineering from the University of Tehran
[53] J. Chen, H. Zeng, C. Hu, and Z. Ji, “Optimization between security                                   in 2005, the M.Sc. degree in computer and commu-
     and delay of quality-of-service,” J. Netw. Comput. Appl., vol. 34, no. 2,                            nication networks from Telecom SudParis in 2007,
     pp. 603–608, 2011.                                                                                   and the Ph.D. degree in computer science from
[54] W. Du, W. Jia, G. Wang, and W. Lu, “Analysis of channel allocation                                   Michigan State University in 2012. His research
     scheme for wireless cellular networks,” Int. J. Ad Hoc Ubiquitous                                    interests include Internet, network security, and
     Comput., vol. 4, nos. 3–4, pp. 201–209, 2009.                                                        large-scale systems.
[55] H. Gao, T. Lv, S. Zhang, C. Yuen, and S. Yang, “Zero-forcing
     based MIMO two-way relay with relay antenna selection: Transmission
     scheme and diversity analysis,” IEEE Trans. Wireless Commun., vol. 11,
     no. 12, pp. 4426–4437, Dec. 2012.
                                                                                                          Joshua W. Hulst received the B.S. degree in com-
[56] X. Guo, D. Zhang, K. Wu, and L. M. Ni, “MODLoc: Localizing multiple                                  puter science from Grand Valley State University
     objects in dynamic indoor environment,” IEEE Trans. Parallel Distrib.                                in 2010 and the M.S. degree in computer science
     Syst., vol. 25, no. 11, pp. 2969–2980, Nov. 2014.                                                    from Michigan State University in 2012. He is
[57] Y. He and X. Wang, “An ALOHA-based improved anti-collision algo-                                     currently a Managing Partner with Michigan Labs,
     rithm for RFID systems,” IEEE Wireless Commun., vol. 20, no. 5,                                      Grand Rapids, MI, a software consulting com-
     pp. 152–158, 2013.                                                                                   pany specializing in mobile app development. His
[58] H. Li, K. Wu, Q. Zhang, and L. M. Ni, “CUTS: Improving channel                                       research interests include networking and security.
     utilization in both time and spatial domain in WLANs,” IEEE Trans.
     Parallel Distrib. Syst., vol. 25, no. 6, pp. 1413–1423, Jun. 2014.
[59] J.-Q. Li, S.-Q. He, Z. Ming, and S. Cai, “An intelligent wireless sensor
     networks system with multiple servers communication,” Int. J. Distrib.
     Sensor Netw., vol. 7, Dec. 2015, Art. no. 960173.
                                                                                                          Zihui Ge received the B.A. degree in computer
[60] J. Qiao, X. S. Shen, J. W. Mark, Q. Shen, Y. He, and L. Lei, “Enabling
                                                                                                          science and technology from Tsinghua University,
     device-to-device communications in millimeter-wave 5G cellular net-
                                                                                                          Beijing, China, in 1998, the M.S. degree in computer
     works,” IEEE Commun. Mag., vol. 53, no. 1, pp. 209–215, 2015.
                                                                                                          science from Boston University, Boston, MA, USA,
[61] B. Wang, J. Chen, S. Jia, C. Lv, X. Wang, and H. Zheng, “Secret sharing                              in 2000, and the Ph.D. degree in computer science
     scheme with dynamic size of shares for distributed storage system,”                                  from the University of Massachusetts, Amherst, MA,
     Secur. Commun. Netw., vol. 7, no. 8, pp. 1245–1252, Aug. 2014.                                       USA, in 2003. He is currently a Principal Member
[62] G. Wang, K. Wu, and L. M. Ni, “CSMA/SF: Carrier sense multiple                                       of the Technical Staff with the Networking and
     access with shortest first,” IEEE Trans. Wireless Commun., vol. 13, no. 3,                           Services Research Center, AT&T Labs–Research,
     pp. 1692–1702, Mar. 2014.                                                                            Florham Park, NJ, USA. His research interests
[63] L. Wang, K. Wu, J. Xiao, and M. Hamdi, “Harnessing frequency domain                                  include IP network management, traffic analysis and
     for cooperative sensing and multi-channel contention in CRAHNs,”             anomaly detection, network data mining, and network security.
     IEEE Trans. Wireless Commun., vol. 13, no. 1, pp. 440–449, 2014.
[64] Q. Yan, F. R. Yu, Q. Gong, and J. Li, “Software-defined networking                                   Dan Pei received the B.S. and M.S. degrees from
     (SDN) and distributed denial of service (DDoS) attacks in cloud com-                                 Tsinghua University in 1997 and 2000, respectively,
     puting environments: A survey, some research issues, and challenges,”                                and the Ph.D. degree from UCLA in 2005. He is
     IEEE Commun. Surv. Tuts., vol. 18, no. 1, pp. 602–622, 1st Quart.,                                   currently a Researcher with AT&T Labs–Research,
     2016.                                                                                                Florham Park, NJ, USA. His current research inter-
[65] Q. Yan and F. Yu, “Distributed denial of service attacks in software-                                ests are network measurement and security.
     defined networking with cloud computing,” IEEE Commun. Mag.,
     vol. 53, no. 4, pp. 52–59, Apr. 2015.
[66] D. Zhang, Y. Liu, X. Guo, and L. M. Ni, “RASS: A real-time,
     accurate, and scalable system for tracking transceiver-free objects,”
     IEEE Trans. Parallel Distrib. Syst., vol. 24, no. 5, pp. 996–1008,
     May 2013.                                                                                            Jia Wang received the Ph.D. degree in com-
[67] D. Zhang et al., “Fine-grained localization for multiple transceiver-free                            puter science from Cornell University in 2001.
     objects by using RF-based technologies,” IEEE Trans. Parallel Distrib.                               She joined AT&T Labs–Research since then and is
     Syst., vol. 25, no. 6, pp. 1464–1475, Jun. 2014.                                                     now a Principal Technical Staff Member with the
[68] Q. T. Zhang, J. Chen, and H. Zhu, “Network convergence: Theory,                                      Network Measurement and Engineering Research
     architectures, and applications,” IEEE Wireless Commun., vol. 21, no. 6,                             Department. Her research interests focus on network
     pp. 48–53, Dec. 2014.                                                                                measurement and management, network security,
                                                                                                          performance analysis and troubleshooting, IPTV,
[69] Y. Zhou, T. Z. J. Fu, and D. M. Chiu, “A unifying model and analysis of
                                                                                                          social networks, and cellular networks.
     P2P VoD replication and scheduling,” IEEE/ACM Trans. Netw., vol. 23,
     no. 4, pp. 1163–1175, Aug. 2015.
