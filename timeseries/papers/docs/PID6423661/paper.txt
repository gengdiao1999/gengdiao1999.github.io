Unsupervised Clustering through Gaussian Mixture
Variational AutoEncoder with Non-Reparameterized
      Variational Inference and Std Annealing
           Zhihan Li† , Youjian Zhao‡ , Haowen Xu† , Wenxiao Chen† , Shangqing Xu† , Yilin Li† , Dan Pei‡∗
                                                        Tsinghua University
                        Beijing National Research Center for Information Science and Technology (BNRist)
                                                           Beijing, China
                             †
                               {lizhihan17, xhw15, chen-wx17, xsq17, liyilin16}@mails.tsinghua.edu.cn
                                              ‡
                                                {zhaoyoujian, peidan}@tsinghua.edu.cn



   Abstract—Clustering has long been an important research        the learned centroids. However, the clustering accuracy of DEC
topic in machine learning, and is highly valuable in many         is also poor, since the training objective of the model does not
application tasks. In recent years, many methods have achieved    match so well with the goal of classical clustering technique.
high clustering performance by applying deep generative models.
In this paper, we point out that directly using q(z|y, x) instead    Another approach, quite different from DEC, is deep gener-
of resorting to the mean-field approximation (as is adopted in    ative models, including variants of Variational Auto-Encoder
previous works) in Gaussian Mixture Variational Auto-Encoder      (VAE) [4] and Generative Adversarial Network (GAN) [5].
can benefit the unsupervised clustering task. We improve the      They are able to model the clustering assignment as a latent
performance of Gaussian Mixture VAE, by optimizing it with a      variable, which benefits the unsupervised clustering task [6]–
Monte Carlo objective (including the q(z|y, x) term), with non-
reparameterized Variational Inference for Monte Carlo Objec-      [8]. i.e., the clustering assignment is also a part of the latent
tives (VIMCO) method. In addition, we propose std annealing       representations in the deep generative model based methods.
to stabilize the training process and empirically show its effects   Along this direction, several methods for unsupervised clus-
on forming well-separated embeddings with different variational   tering were proposed. Gaussian Mixture Generative Adversarial
inference methods. Experimental results on five benchmark         Network (GM-GAN) [6] proposed a variant of GAN where the
datasets show that our proposed algorithm NVISA outperforms
several baseline algorithms as well as the previous clustering
                                                                  probability distribution over the latent space is a mixture of
methods based on Gaussian Mixture VAE.                            Gaussians. However, for the GAN-based models, the constraints
   Index Terms—Unsupervised Clustering, Gaussian Mixture          on the relationship between the clustering assignment latent
Variational Auto-Encoder, Std Annealing                           variable and other latent variables is not very strong, which
                                                                  may downgrade the clustering performance on datasets more
                      I. I NTRODUCTION                            complicated than MNIST (see Section III-B). As for VAE-
   Clustering is a fundamental and important research topic based models,     1
                                                                                       Gaussian Mixture Variational Auto-Encoder
in machine learning and artificial intelligence, which aims at    (GMVAE        )  [7] and Variational Deep Embedding (VaDE) [8]
grouping similar examples together in an unsupervised manner.     both  proposed      variants  of Gaussian Mixture VAE, which have
Traditional clustering methods like k-means [1] and Gaussian      better  constraints     on  the  relationship between the clustering
Mixture Models (GMMs) [2] represent the clusters using simple     assignment      latent variable  (categorical latent variable) and other
similarity measure and statistical distributions in data space or latent variables.    Both  of them  applied mean-field   approximation,
hand-crafted feature space, which need careful feature designs in order to more easily deduce their training methods, based
for specific tasks. However, it’s quite challenging to manually on Stochastic Gradient Variational Bayes (SGVB) estimator
design such features for complex data such as images, resulting and reparameterization trick [4].
in poor clustering performance.                                   Our Approach. Gaussian Mixture VAE is appealing for
Related Work. In recent years, with the help of deep neural clustering, since among all models, the VAE-based models can
networks (DNN), many researches have shown that learning better characterize the constraints on the relationship between
good representations plays an important role in improving different types of latent variables, useful in the unsupervised
the accuracy of clustering on complex datasets. For example, clustering task which involves disentangling the categorical
Deep Embedded Clustering (DEC) [3] proposed a method and other information of the input. However, the performance
to simultaneously learn feature representations and clustering of the previous works (GMVAE [7] and VaDE [8]) seems not
centroids using DNNs, then applied simple k-means clustering satisfactory. We noticed that both these works used the mean-
on the low-dimensional feature space derived from DNN with          1 We use Gaussian Mixture VAE to denote the VAE-based model with
                                                                      Categorical latent variable y and continuous Gaussian latent variable z
  * Dan Pei is the corresponding author.                              depending on y, while GMVAE specifically denotes the previous work [7].
                                                    ✓
                                                                                   where each variable is derived as:
                                       N                        N
                   x        z      y        x       z       y                                                 pθ (y) = Cat(π)                            (2a)
                                                                                                                                   2
                                                                                                                                            
                                                                                                  pθ (z|y) = N µz (y; θ), σz (y; θ) I                    (2b)
                                                                                                  pθ (x|z) = N µx (z; θ), σx 2 (z; θ) I
                                                                                                                                             
Fig. 1. Architecture of NVISA. The Categorical latent variable y as well as                                                                              (2c)
the continuous Gaussian latent variable z are regarded as part of the generative
model (right), the left one denotes the variational model.                          We let the prior distribution pθ (y) = Cat(π) assign equal
                                                                                   probability to each possible choice of y ∈ {1 . . . K}. pθ (z|y)
field approximation, which we advocate should be replaced                          is assumed to be a Gaussian distribution, with µz (y; θ) as its
by directly using q(z|y, x) (as shown in Fig. 1). In this                          mean and σz (y; θ) as its standard deviation (std). pθ (x|z) is
paper, we propose NVISA (Gaussian Mixture VAE with Non-                            also assumed to be a Gaussian distribution, derived in a way
reparameterized Variational Inference and Std Annealing ), an                      similar to z. µz (y; θ) and σz (y; θ) are just learnable vectors
unsupervised clustering method based on Gaussian Mixture                           for each y, respectively. µx (z; θ) and σx (z; θ) are derived
VAE without mean-field approximation, using Variational                            by neural networks, with parameters θ. Note that we add a
Inference for Monte Carlo Objectives (VIMCO) [9] as training                       constant lower-bound to the std of prior pθ (z|y):
                                                                                                     0
method and the standard deviation (std) annealing trick (i.e.,                                    σz (y; θ) = SoftPlus(σz (y; θ)) + 1
gradually shrink the lower bound of std of the posteriors during
training) proposed by us.                                                          where SoftPlus(a) = log(exp(a) + 1). This is because we
   The main contributions of the paper are:                                        want the prior pθ (z|y) (which is learnable) to be at least as
   • We point out that directly using q(z|y, x) instead of                         large as a unit Gaussian.
      using the mean-field approximation in previous works can                        To apply variational inference on pθ (x, y, z), we need a sep-
      benefit the unsupervised clustering task with Gaussian                       arated qφ (z, y|x) to approximate the true posterior pθ (z, y|x).
      Mixture VAE.                                                                 We factorize the variational posterior as:
   • We experiment with three possible variational inference                                         qφ (z, y|x) = qφ (z|y, x)qφ (y|x)                    (3)
      methods (EnumY, NVIL, VIMCO) to train NVISA, and
      finally use the non-reparameterized method VIMCO to                          where each variable is derived as:
      achieve the best performance. Besides, we propose std                                              qφ (y|x) = Cat(π(x; φ))                         (4a)
      annealing to stabilize the training of NVISA with VIMCO,                                                                         2
                                                                                                                                         
      and empirically show that the std annealing can help                                   qφ (z|y, x) = N µz (x, y; φ), σz (x, y; φ) I                (4b)
      form well-separated embeddings and improve clustering     qφ (y|x) is assumed to be a Categorical distribution, while
      accuracy with different variational inference methods.    qφ (z|y, x) is assumed to be a Gaussian distribution. π(x; φ),
   • We evaluate NVISA with VIMCO and std annealing on          µz (x, y; φ) and σz (x, y; φ) are all derived by neural networks
      five benchmark datasets. Our results show that NVISA      with parameters φ. Note that, we also add a lower bound to
      outperforms several baseline clustering algorithms as wellthe σx (z; θ) and σz (x, y; φ) to stabilize the training of our
      as the previous clustering methods based on Gaussian      model, see Section II-D for more details.
      Mixture VAE.                                                 The whole architecture of our model, including pθ (x, y, z)
        II. NVISA FOR UNSUPERVISED CLUSTERING                   and qφ (z, y|x), is shown as Fig. 1. In the generative network, x
                                                                conditions on z and z conditions on y, hence the latent variable
   In this section, we first present the architecture of NVISA.
                                                                z should encode the label information. Thus, it’s natural to let
Unlike the previous works [7], [8], NVISA does not adopt
                                                                z condition on both x and y, i.e., directly calculating qφ (z|y, x)
the mean-field approximation. We then show how to optimize
                                                                rather than using the mean-field approximation qφ (z, y|x) =
NVISA with three candidate variational inference methods
                                                                qφ (z|x)qφ (y|x).
(EnumY, NVIL and VIMCO). Finally, we propose the std
annealing, which helps stabilize the training process of NVISA B. Using qφ (z|y, x) instead of Mean-Field Approximation
when VIMCO is used, resulting in better embeddings in the          The previous works that use Gaussian Mixture VAE on
latent space with different variational inference methods.      unsupervised clustering [7], [8] both adopt mean-field ap-
A. Architecture                                                                    proximation qφ (z, y|x) = qφ (y|x) qφ (z|x). By doing this,
                                                                                   they managed to train their model without sampling y, the
  As in [7], [8], we use a Categorical latent variable y ∈
                                                                                   categorical latent variable, which is impossible to directly
{1 . . . K} to represent the clustering assignment of a given
                                                                                   train2 with SGVB and reparameterization trick. However, this
input x, and a continuous Gaussian latent variable z, which
                                                                                   mean-field approximation qφ (y|x) qφ (z|x) can only represent
depends on y, to capture other information of x. The whole
                                                                                   joint distributions where z and y are conditionally independent
generative model is thus composed of three variables, namely
x, y and z, with parameters denoted as θ. We factorize the                            2 Although it’s possible to use SGVB and categorical reparameterization
generative model as follows:                                                       with Gumbel-Softmax [10] to train Gaussian Mixture VAE with categorical
                                                                                   latent variable, this method can only get around 80% clustering accuracy on
                 pθ (x, y, z) = pθ (x|z) pθ (z|y) pθ (y)                    (1)    MNIST in our experiments. Thus we do not discuss this method in our paper.
given x, which may not hold in the real-world datasets in many       lower bound to train non-reparameterized latent variables. They
cases. On the contrary, the factorization qφ (z|y, x)qφ (y|x) can    suggested to take advantage of the multiple samples, in order
represent all possible joint distributions qφ (z, y|x), regardless   to eliminate the need of using the separated learnable network
of whether or not z and y are conditionally dependent                Cψ (x) and constant c (as in NVIL). This non-reparameterized
given x. Thus we choose the factorization qφ (z|y, x)qφ (y|x)        variational inference method is called VIMCO. In NVISA, the
deliberately. We shall see in Section III-C that, our model,         Monte Carlo objective can be formulated as (7).
which directly calculates qφ (z|y, x), significantly outperforms
GMVAE, VaDE, and a variant of our model which uses mean-                LM (x; θ, φ) =
                                                                                                   "           M
                                                                                                                                        #
field approximation instead.                                                                               1 X pθ (x, y (m) , z(m) )
                                                                        E(y,z)(1:M ) ∼qφ (z,y|x)       log                                  (7)
C. Three Candidate Variational Inference Methods for NVISA                                                 M m=1 qφ (z(m) , y (m) |x)
    Since we decide not to use the mean-field approximation,         where (y, z)(1:M ) are M independent joint samples of (y, z)
we need a new variational inference method, different from           from qφ (y, z|x), and each (y, z) is sampled by first taking
the previous work, to train NVISA. In order to deal with             y ∼ qφ (y|x), then z ∼ qφ (z|y, x). The details of VIMCO
the non-reparameterized Categorical latent variable y, we            gradient estimator can be found in [9].
found three candidate methods: the EnumY, which enumerates              Although more stable than NVIL, the VIMCO method may
y and uses SGVB [4], [11], and two non-reparameterized               still cause loss divergence in a few tens or hundreds of epochs
method, NVIL [12] and VIMCO [9]. These methods are briefly           in our clustering task (see Section III-D). We developed the
described as follows.                                                std annealing trick to stabilize training with VIMCO. With
EnumY In Gaussian Mixture VAE, if we enumerate y =                   this trick, training with VIMCO converges and gives us better
1 . . . K and keep the z variable reparameterized, the evidence      models than EnumY. We thus choose VIMCO as the training
lower bound (ELBO) can be written into a summation over              method for NVISA.
y and an expectation over z (5), which can be optimized by
SGVB with reparameterization trick [4], [11]:                        D. The Std Annealing Trick
                        K
                        X                                              We developed the std annealing trick (i.e., add and gradually
    LELBO (x; θ, φ) =         qφ (y|x) Eqφ (z|y,x)                   shrink the lower bound of std of the Gaussian posteriors during
                        y=1                                          training) to stabilize the model training with VIMCO and
                                                                     Gaussian pθ (x|z). Moreover, we empirically show that, std
                                                          
         log pθ (x, y, z) − log qφ (y|x) − log qφ (z|y, x)    (5)
                                                                     annealing can help learn better embeddings and lead to higher
Unfortunately, this simplest method does not work as well as         clustering performance on the raw image datasets like MNIST
the non-reparameterized method, and is likely to get “mixture        and fashion-MNIST, either with EnumY or VIMCO training
models” (see Section III-D).                                         method. The insights of developing this trick is shown below.
NVIL Mnih and Gregor [12] proposed Neural Variational                   In our experiments, we observed that the loss may diverge
Inference and Learning (NVIL) to estimate the gradient               only if 1) we assume pθ (x|z) to be a Gaussian distribution;
of ELBO, when latent variables are NOT reparameter-                  and 2) we use NVIL or we use VIMCO method with just
ized. Let f (x, y, z) = log pθ (x, y, z) − log qφ (z, y|x), and      a small number of samples. If we assume pθ (x|z) to be a
LELBO (x; θ, φ) = Eqφ (z,y|x) [f (x, y, z)], NVIL then estimates     Bernoulli distribution (which is more stable than Gaussian in
the gradient ∇LELBO (x; θ, φ) by (6):                                our context), the loss will not diverge. However, the Bernoulli
                                                                     pθ (x|z) is only applicable to a few datasets (e.g., MNIST),
                                      
    ∇ LELBO (x; θ, φ) = Eqφ (z,y|x) ∇f (x, y, z)
                                                                    and in our experiments, it does not work as well as our final
          + (f (x, y, z) − Cψ (x) − c) ∇ log qφ (z, y|x)     (6)
                                                                     model using Gaussian pθ (x|z) even on such datasets. Thus we
Cψ (x) is derived by another trainable neural network (which         have to develop tricks that make the training with Gaussian
is called “baseline net”) with parameter ψ, while c is a             pθ (x|z) converge. Unfortunately, we cannot find a trick that
learnable constant. The gradient (6) is directly used to train the   can stabilize NVIL with Gaussian pθ (x|z). For VIMCO with
parameters θ, φ and ψ with stochastic gradient descent, instead      Gaussian pθ (x|z), using a large number of samples may help
of computing the gradient of a certain objective function.           the loss converge, but we found that it takes too long for
   In our experiments (Section III-D), the variance of NVIL          training and its performance is not as good as VIMCO with
gradient estimator is too large (which is theoretically possible),   a small number of samples and the stabilization trick that we
such that when training with NVIL, the loss of the model             managed to propose below, which turns out to also work better
would diverge.                                                       than EnumY (see experimental results in Section III-D).
VIMCO Burda et al. [13] proposed the Monte Carlo objective              Our proposed stabilization trick is based on the following
for training VAEs. It is a tighter variational lower bound than      observations and analysis. At the beginning of training, we
ELBO, computed from multiple samples of latent variables.            observe huge loss and gradients with large variance, and then
The original method of [13] requires the latent variables to be      the loss diverges. We then try to clip the gradients by L2-norm,
reparameterized, such that the gradient can be estimated by an       but the loss still gradually increases, and eventually reaches
SGVB-like algorithm. Mnih and Rezende [9] then extended this         ∞. This suggests that the loss divergence is not because the
gradients are too large, but because, when the loss has large            and fashion-MNIST, either with EnumY or VIMCO training
variance, the gradients often guide the optimization to wrong            method. In NVISA, the ELBO can be written as:
directions. Looking closer into the Monte Carlo objective (7),
we can see that it is just a lower-bound of the importance                 LM (x; θ, φ) =Eqφ (z,y|x) pθ (x|z)
sampling based estimator for log pθ (x):                                                     − DKL (qφ (y, z)kpθ (y, z)) − I((Z, Y ); X)
                           "          M
                                                             #
                                  1 X pθ (x, y (m) , z(m) )       where the first term is called reconstruction loss, the second
   E(y,z)(1:M ) ∼qφ (z,y|x) log                                   term is KL divergence between posterior and prior, the last term
                                  M m=1 qφ (z(m) , y (m) |x)
                                   "                            # (mutual information) acts as a regularizer [15]. During training,
                                         M
                                     1 X pθ (x, y (m) , z(m) )    the model pays more attention to optimize the reconstruction
    ≤ log E(y,z)(1:M ) ∼qφ (z,y|x)                                loss (which is much larger than the other two terms) rather
                                     M m=1 qφ (z(m) , y (m) |x)
                       "                   #                      than the KL term, while the latter one actually makes the
                         pθ (y, z|x)                              posterior match the Gaussian mixture prior and helps form
    = log Eqφ (z,y|x)                pθ (x) = log pθ (x)
                         qφ (z, y|x)                              better embeddings for clustering. Thus, for the raw image data
                                                                  where some images from different classes might be similar, it’s
The theory of importance sampling states that, the variance of easy to get “mixture” clusters on the latent space. However,
this estimator will be large if qφ (z, y|x) does not approximate at the early stage of training, a large lower bound on the
pθ (z, y|x) well enough [14]. This is exactly what we will std of p (x|z) actually slows down the optimization of the
                                                                            θ
encounter at the beginning of training, when pθ (z, y|x) are reconstruction loss, and the regularizer (whose value is very
still rapidly evolving. Thus qφ (z, y|x) can never approximate small) only changes slightly, pushing the model to optimize the
the true posterior well. The large variance on the importance KL term and make the posterior match the Gaussian mixture
sampling estimator may cause large variance on the objective prior. Note that, the std annealing is applied during training
function, and may further cause the gradients go towards wrong process, rather than modify the training objective (which may
directions. When using Gaussian pθ (x|z), for NVIL, the large harm the final reconstruction). We empirically show that this
variance may be caused by the incapability of baseline net. leads to a better-separated embeddings, which significantly
The EnumY does not use Monte Carlo objective, thus will not improves the clustering performance with different variational
have this problem. When using VIMCO with a large number inference methods in Section III-D.
of samples, the variance is reduced, thus the training converges.
However, as discussed above, these two method does not lead                      III. E XPERIMENTS AND A NALYSIS
to good performance. We then try to find additional tricks to A. Setup
prevent the loss divergence when using Gaussian pθ (x|z) and
VIMCO with a small number of samples.                             Datasets We evaluate NVISA on five benchmark datasets
   Since pθ (x|z) and qφ (z|y, x) are Gaussian distributions, and compare its performance with several baseline clustering
adding large lower-bounds to their stds actually limit the scale methods to show the superiority of our algorithm. The datasets
of the probability densities, which prevent the scale of Monte include: MNIST [16], fashion-MNIST [17], STL-10 [18],
Carlo objective from becoming too large when the importance CIFAR-10 and CIFAR-100 [19]. MNIST and fashion-MNIST
sampling estimator still has large variance (which causes the are grayscale image datasets about hand-written digits and
loss diverges). Based on these observations and analysis, we fashion clothes, while the other three datasets are color natural
propose our std annealing trick as follows: We add large images. Previous works [8], [20] have found that raw pixels
lower-bounds on the standard deviation (std) of the Gaussian of these complex color images may not be suited for our
distributions pθ (x|z) and qφ (z|y, x) at the beginning of the clustering task, since the color information would be dominant,
training, gradually shrink the lower-bounds during training but unrelated to clustering. Thus, following previous works [6],
process, and finally set them to small constants at the end of [8], [21], we use raw pixel values 3 for MNIST and fashion-
training. The lower-bounds are added as:                          MNIST, 2048-dimensional features extracted by pre-trained
                0
                                                                  Resnet-50 [22] (which is trained on ImageNet [23]) for STL-10,
             σx (z; θ) = SoftPlus(σx (z; θ)) + x                 CIFAR-10 and CIFAR-100. These configurations are used for
             0
           σz (x, y; φ) = SoftPlus(σz (x, y; φ)) + z             all the compared methods.
                                                                  Evaluation Metric It is a common practice to use a K-
where x and z are annealing parameters, initialized to 1, class labeled classification dataset to evaluate the clustering
shrunk by 0.5 after every 100 epochs, and set to 10−4 at performance of a model, by using the unsupervised clustering
the end. With the std annealing trick, the loss never diverges accuracy (ACC for short hereafter) [3], [6], [8], [21]:
when training NVISA with VIMCO in our experiments in                                                 PN
Section III-B. The resulting model achieves much higher                                                      I(f (ci ) = li )
                                                                                  ACC = max i=1
clustering accuracy than the previous Gaussian mixture VAE-                                    f ∈F             N
based methods on several benchmark datasets.                        3 The raw image is resized through a bilinear filter and then passed through
   Moreover, we found that the std annealing can help learn the pre-trained Resnet-50. A 7 × 7 average pooling is applied on the last
better embeddings on the raw image datasets like MNIST feature map of Resnet-50 to get the 2048-dimensional features.
N is the total number of samples. ci is the clustering assignment     one single y variable, while LTVAE-full performs structure
for the i-th testing data, produced by the model, while li is         learning to learn the superstructure through a latent tree model.
the ground-truth class label. F is the set of all possible one-       The use of multiple latent variables and the learned structure
to-one mappings between cluster assignments and labels. The           help LTVAE-full achieves good clustering performance on
best mapping f (as required by max) can be obtained using             several datasets. However, although not using the complex
KuhnMunkres algorithm [24]. Following previous work, we               superstructure, NVISA achieves clustering results better than
always use K, the number of ground-truth classes, as the              or comparable to LTVAE-full on all the five datasets. Each
number of clusters to fit. Note that K = 100 for CIFAR-100,           experiment of NVISA takes 3-12 hours to train on one GTX
and K = 10 for other datasets.                                        1080 Ti graphical card for every dataset except CIFAR-100.
Other Configurations The network structure is shown in                The training time of CIFAR-100 is around 30 hours, since
Fig. 5(a), while the hidden layers consist of several convolu-        more VIMCO samples are needed for this 100-class dataset.
tional and deconvolutional layers. We use Adam [25] optimizer         Overall, such training overhead is comparable with other deep
to optimize our model. The initial learning rate is set to 0.001,     generative model based methods.
with a decay rate of 0.5 every 300 epochs. The number of                 One thing to mention is that, the large standard deviation
VIMCO samples M is set to 50 for CIFAR-100, and 15 for all            (2.64%) of our method on MNIST dataset is mainly caused by
other datasets. According to [26], a uniform random noise is          one run with around 90% accuracy. This might be caused by
added on the raw pixel images for training, and then normalize        the bad initialization which makes the model mix up similar
the pixel value to [0, 1] (by dividing 256). For other datasets,      numbers like “4” and “9”. However, inspired by [8], we find that
the features are standardized to 0 mean and 1 variance as input.      using parameters of a simple pretrain model as an initialization
According to [27], the latent z dimension should not be very          of NVISA can help tackle the above problem. Specifically, we
large, in order to get more disentangled representations for          pretrain an AutoEncoder who has the same network structure
clustering task. In this paper, we use z dimension L = 10 for         as NVISA, then perform GMM on the latent space obtained by
MNIST, CIFAR-10 and CIFAR-100, L = 16 for more complex                AE. The network parameters are used to initialize the neural
fashion-MNIST dataset, and L = 5 for the simplest STL-10              networks in NVISA, while the means of GMM are used as
features. The cluster assignment of each input x is obtained          the initial means of z priors in NVISA (instead of random
by finding the y with the maximum qφ (y|x).                           initialization). With this better initialization, NVISA is able to
                                                                      get 98.04%(±0.17%) clustering accuracy on MNIST (evaluated
B. Overall Performance                                                in ten runs).
   We evaluated our method NVISA on five benchmark
datasets, and compared it with several baseline clustering meth-      C. qφ (z|y, x) vs Mean-Field Approximation
ods, including AE+GMM (AutoEncoder + Gaussian Mixture                    Among the three variants of Gaussian Mixture VAE, NVISA
Model), VAE+GMM, DEC [3], GM-GAN [6], GMVAE [7],                      significantly outperforms GMVAE and VaDE, as shown in
VaDE [8], LTVAE-GMM [21] and LTVAE-full [21] (LTVAE                   Table I. Unlike the previous works, we deliberately use
with structure learning in latent space). The results are shown       qφ (z|y, x), instead of the mean-field approximation, as dis-
in Table I. We report the means and standard deviations in            cussed in Section II-B. Our experiments on MNIST (Table II)
ten runs for our method NVISA and highlight the top two               shows that the model using qφ (z|y, x) (adopted in NVISA
clustering accuracy on each dataset.                                  with VIMCO and std annealing) outperforms the models
   Our NVISA overall performs the best among all the                  using mean-field approximation qφ (z, y|x) = qφ (z|x)qφ (y|x).
compared methods on five benchmark datasets. Specifically,            This is verified not only by the results of previous works,
NVISA significantly outperforms GMVAE and VaDE, which                 but also by the controlled experiment on NVISA with mean-
are based on variants of Gaussian Mixture VAE. This highlights        field approximation. Fig. 4 also shows that the embeddings
the benefits of our modifications (qφ (z|y, x), VIMCO and std         learned by NVISA are better separated than the controlled
annealing) to the Gaussian Mixture VAE. Note that, GMVAE              experiment with mean-field approximation. Thus, we stick to
uses an additional regularization term to help improve its            using qφ (z|y, x) in NVISA.
clustering performance, which is not very applicable to our
model. However, even without using this regularization term,          D. Comparison of Different Variational Inference Methods and
NVISA still outperforms GMVAE on all the five datasets. Thus,         Std Annealing
we decide not to use any clustering specific regularization              Table II shows the unsupervised clustering accuracy on
term in NVISA. Besides, the result of NVISA on MNIST is               MNIST of previous works, and NVISA trained with three
slightly worse than GM-GAN. However, NVISA outperforms                variational inference methods (EnumY, NVIL and VIMCO)
GM-GAN on the more complicated fashion-MNIST dataset,                 and other configurations. “(Bernoulli)” indicates that a model
which suggests NVISA should be at least no worse than GM-             uses Bernoulli pθ (x|z), otherwise it uses Gaussian. “(mean-
GAN. LTVAE [21] (Latent Tree Variational AutoEncoder) is              field)” indicates that a model uses the mean-field approximation,
a Variational AutoEncoder with a superstructure of discrete           otherwise it uses qφ (z|y, x). “(1000 samples)” indicates that
latent variables (i.e., y) on top of the latent features (i.e., z).   a model uses 1000 samples in VIMCO, otherwise it uses
LTVAE-GMM uses a fixed Gaussian mixture structure with                15. “*” indicates the std annealing is applied. Note that,
                                                               TABLE I
                           C OMPARISON OF UNSUPERVISED CLUSTERING ACCURACY (%) ON FIVE BENCHMARK DATASETS .

                               Method             MNIST        Fashion-MNIST             STL-10                     CIFAR-10             CIFAR-100
                              AE+GMM               84.27              53.83            76.18          43.66          29.41
                              VAE+GMM              76.87              53.43            82.42           59.4          30.81
                               DEC [3]            84.30∗              57.58            84.08          70.31          31.22
                             GM-GAN [6]           99.24 ∗            58.16∗              -              -              -
                              GMVAE [7]           88.54∗              50.15            65.72          49.79          14.46
                               VaDE [8]           94.46∗              56.12           84.45∗          59.69          23.58
                          LTVAE-GMM [21]           86.31              55.69            81.66          63.51          16.98
                           LTVAE-full [21]        86.32 ∗             61.32           90.00 ∗         69.16          38.52
                             NVISA(best)           98.40              66.14            91.22          76.96          38.79
                           NVISA(avg±std)       96.55±2.64        62.63±2.92        89.15±2.36     71.37±2.79     37.32±0.70
                          Results marked by ∗ are excerpted from their original paper.
                          “-” means there is no published result and we didn’t find any released code to produce it.


                                                                                                         2000                    VIMCO15_anneal                     EnumY_no_anneal   500
                             TABLE II




                                                                                   loss(Gaussian)




                                                                                                                                                                                          loss(Bernoulli)
                                                                                                                                 VIMCO15_Bernoulli_no_anneal        VIMCO1000_no_anneal
         T HE UNSUPERVISED CLUSTERING ACCURACY ON MNIST.                                                 1000                                                                         400
                                                                                                            0                                                                         300
                                                                                                         1000                                                                         200
  Variational Inference   Method                             ACC (%)
                                                                                                         2000                                                                         100
                          GMVAE                                 88.54                                    3000                                                                         0
                          VaDE                                  94.46                                           0   100000       200000           300000          400000          500000




                                                                                   loss (symlog scale)
                                                                                                         1040                VIMCO15_no_anneal         NVIL_no_anneal       NVIL_anneal
  EnumY                   NVISA (Bernoulli)                     95.12                                    1032
                          NVISA                                 78.43                                    1024
                                                                                                         1016
                          NVISA∗                                93.81                                     108
                                                                                                            0
  NVIL                    NVISA                                   N/A
                                                                                                          108
                          NVISA∗                                  N/A                                    1016
                                                                                                                0   100000       200000           300000          400000          500000
  VIMCO                    NVISA                                 N/A                                                                      step
                           NVISA (Bernoulli)                    95.74
                           NVISA (1000 samples)                 71.42        Fig. 2. Training loss of NVISA with different Variational Inference methods
                           NVISA∗ (1000 samples)                 91.3        on MNIST. The loss curves are smoothed for better visualization. Only 200,000
                           NVISA∗ (mean-field)                  95.25        steps are used for VIMCO-1000 since it takes too long to train. (Top) The
                           NVISA∗                               98.40        right y-axis corresponds to the experiment with Bernoulli pθ (x|z), while the
  “*” indicates the std annealing is applied.                                left y-axis corresponds to others with Gaussian pθ (x|z). (Bottom) shows
  “N/A” means the loss diverges and we cannot get a meaningful result.       the diverged models discussed in Section III-D.



as discussed in Section II-D, although it’s applicable to use
Bernoulli pθ (x|z) for MNIST, we can only achieve 95.74%
accuracy (with VIMCO) and 95.12% (with EnumY). Besides,
Bernoulli assumption is not applicable for most datasets. Thus,
we stick to use Gaussian pθ (x|z) in NVISA for all datasets.                             (a)                (b)

   As shown in Fig. 2, training NVISA using VIMCO 15
samples without std annealing, and using NVIL with or without
std annealing, all suffer from loss divergence. This is because
the importance sampling estimators have large variance, as
discussed in Section II-D. However, with the std annealing,
NVISA with VIMCO 15 samples can be properly trained and                                  (c)                (d)

achieve the best clustering performance.
   One thing to notice is that, directly using Gaussian with Fig.    3. Generated samples using NVISA with different variational inference
                                                                 methods on MNIST. (a) EnumY, (b) VIMCO with 1000 samples, (c) EnumY
EnumY or with VIMCO 1000 samples leads to poor per- with std annealing, (d) VIMCO with 15 samples and std annealing
formance. We found that, this is likely because these two
models degenerate into true “mixture models” from the early
stage in training: only the marginal distribution pθ (z) = x ∼ pθ (x|z). Moreover, although the above two models can
P
   y pθ (z|y)pθ (y) is meaningful, while each individual com- be trained without std annealing, we find that equipped with
ponent pθ (z|y) does not correspond to any particular x. This std annealing trick can also help them learn better embeddings
phenomenon is shown as Fig. 3, where we obtain 100 x samples in the latent space and alleviate the “mixture model” problem.
from each model by ancestral sampling, i.e., each x is produced As shown in Fig. 4, for the 10,000 test-set images on MNIST
by first sampling a y ∼ pθ (y), then a z ∼ pθ (z|y), and finally dataset, we use z ∼ qφ (z) = Ep∗ (x) Σy qφ (z|y, x)qφ (y|x) to
                                                                                           Latent Variable z                                 Reconstructed x
                                                                                             L Dimensions                                    W Dimensions
                                                                                           µz                z                              µx               x
                                                                                      Linear             SoftPlus + ✏z                   Linear          SoftPlus + ✏x
                                                                                      L Units              L Units                       W Units           W Units

                                                                                                                   Latent Variable y
                                                                                                hx     hy                                     Hidden Layers
                                                                                                                    K Dimensions
                                                                                                                                                  f✓ (z)

                                                                                            Hidden Layers                Linear
                                                                                                f (x)                    K Units             Latent Variable z
                                                                                                                                              L Dimensions

                                                                                          Pixels or Features x                              Latent Variable y
                                                                                            W Dimensions                                     K Dimensions


                                                                                         Variational Net                                  Generative Net
                                                                                                                             (a)                                         (b)
              (a) NVISA* (98.40)             (b) NVISA*+mean-field (95.25)

                                                                                     Fig. 5. (a) Network structure of NVISA. Gray nodes are random variables
                                                                                     and white nodes are layers. (b) ACC of NVISA with different parameters on
                                                                                     MNIST. Each configuration is evaluated in five runs.



                                                                            the beginning of the training. During training process, we want
                                                                            the model to converge under each intermediate std lower bound,
                                                                            which empirically helps make the posterior match the Gaussian
                                            (d) NVISA*+EnumY (93.81)        mixture prior, and leads to a better separated embedding for
         (c) NVISA+EnumY (78.43)
                                                                            clustering. Thus, we choose to anneal the std lower bound every
Fig. 4. 10,000 encoded test-set digits and 10,000 samples from the prior on 100 epochs at the rate of 0.5. These configurations empirically
MNIST, dimensionally reduced by t-SNE, colored by model predicted labels. works well on all our five datasets.
“*” indicates the std annealing is applied. Values in parenthesis are the test-set
clustering accuracy.                                                                                                                   IV. R ELATED W ORK
                                                                    Recently, deep learning methods are widely used to learn
get the encoded z (p∗ (x) denotes the true data distribution on better latent representations from the training data for clustering
test set). Besides, another 10,000 samples are obtained from task. To further improve the clustering accuracy, recent meth-
the prior Σy pθ (z|y). These 20,000 samples are dimensionally ods [3], [20], [29] often jointly refine the latent features and
reduced using t-SNE [28]. As shown in Fig. 4 (d), EnumY with clustering assignments, through carefully designed combination
std annealing learns better-separated embeddings and generates of loss functions or additional regularization terms. These
clear digits (Fig. 3 (c)). This is in sharp contrast with Fig. 4 methods achieve higher clustering performance than directly
(c) and Fig. 3 (a), which does not apply std annealing. Similar apply conventional clustering methods on raw data or features,
results are obtained when using VIMCO 1000 samples. It especially for the complex datasets.
empirically shows that, with the std annealing trick, the model     Apart from the above methods, several generative model
learns well-separated embeddings and matches the posteriors to based clustering methods [6]–[8], [21] have been proposed.
the priors better. The std annealing help improve the accuracy These methods jointly train the continuous latent variables and
of EnumY from (78.43%/51.41%) to (93.81%/60.56%) on the clustering assignments through a deep generative model,
(MNIST/fashion), and improve the accuracy of VIMCO 1000 where the models are able to achieve clustering in the latent
samples from 71.42% to 91.30% on MNIST.                           space, as well as generate meaningful samples for each class.
   Fig. 4 (a) and Fig. 3 (d) shows the learned embeddings Some of them are based on GANs [5], e.g., GM-GAN [6]
and generated digits by NVISA with VIMCO 15 samples assumed a mixture of Gaussians prior on the input noise of
and std annealing, which achieves 98.40% clustering accuracy GAN, while training a K-way multi-class classifier with the
on MNIST. It clearly shows that the embeddings are well- samples generated from GAN model to do clustering.
separated in the latent space, each corresponds to a category       Other methods are based on VAEs [4], which are the
of digits in data space. According to the experiments and most relevant works to our approach NVISA in this paper.
analysis above, we conclude that it is adequate to use the LTVAE [21] assumed the dependency among latent variables
non-reparameterized method VIMCO with small number of (i.e., zs and ys) follow a tree structure model. It used a
samples and std annealing to train NVISA, targeted for an latent tree model to perform structure learning to learn the
unsupervised clustering task.                                     superstructure among latent variables, and iteratively improved
                                                                  the structure while learning the representations of data in a
E. Hyper-Parameter Selection                                      greedy manner. GMVAE [7], VaDE [8] and our method NVISA
   We did some extra experiments on MNIST to analyze the can be regarded as different variants of Gaussian Mixture
sensitivity of std annealing to the hyper-parameter selection, VAE, but have significant differences. Both GMVAE and VaDE
as shown in Fig. 5(b). Although the model is somewhat applied mean-field approximation and trained their model with
sensitive to the initial std lower bound and annealing frequency, SGVB [4], [11] and reparameterization trick. Specifically,
it’s easy to choose these parameters based on the following GMVAE used an additional noise input variable w to derive the
intuitions. Similar to the prior, we set the initial std lower GMM prior, as well as an information theoretic regularization
bound to 1 to make each posterior at least a unit gaussian at term to help training. VaDE made an additional approximation,
i.e., assume q(y|x) = p(y|z) in order to train its model and                     [4] D. P. Kingma and M. Welling, “Auto-encoding variational bayes,” 2nd
derive the cluster assignments. However, in NVISA, we point                          International Conference on Learning Representations (ICLR), 2014.
                                                                                 [5] I. Goodfellow, J. Pouget-Abadie, M. Mirza, B. Xu, D. Warde-Farley,
out that directly using qφ (z|y, x) instead of using the mean-                       S. Ozair, A. Courville, and Y. Bengio, “Generative adversarial nets,” in
field approximation can benefit the clustering task, and carefully                   Advances in neural information processing systems, 2014, pp. 2672–2680.
designed the corresponding model structure. In order to deal                     [6] M. Ben-Yosef and D. Weinshall, “Gaussian mixture generative adversarial
                                                                                     networks for diverse datasets, and the unsupervised clustering of images,”
with the non-reparameterized Categorical latent variable y,                          arXiv preprint arXiv:1808.10356, 2018.
NVISA uses VIMCO as the variational inference method.                            [7] N. Dilokthanakul, P. A. Mediano et al., “Deep unsupervised cluster-
Moreover, NVISA proposes the std annealing trick to help                             ing with gaussian mixture variational autoencoders,” arXiv preprint
                                                                                     arXiv:1611.02648, 2016.
the model learn better-separated embeddings, which benefits                      [8] Z. Jiang, Y. Zheng, H. Tan, B. Tang, and H. Zhou, “Variational deep
the unsupervised clustering task. The qφ (y|x) term is directly                      embedding: an unsupervised and generative approach to clustering,” in
trained and derived by neural networks in NVISA, and then                            Proceedings of the 26th International Joint Conference on Artificial
                                                                                     Intelligence. AAAI Press, 2017, pp. 1965–1972.
the cluster assignment of each input x is obtained by directly                   [9] A. Mnih and D. Rezende, “Variational inference for monte carlo
finding the y with the maximum qφ (y|x).                                             objectives,” in ICML, 2016, pp. 2188–2196.
                                                                                [10] E. Jang, S. Gu, and B. Poole, “Categorical reparameterization with
                       V. C ONCLUSION                                                gumbel-softmax,” arXiv preprint arXiv:1611.01144, 2016.
                                                                                [11] D. J. Rezende, S. Mohamed, and D. Wierstra, “Stochastic backprop-
   In this paper, we present the analysis and experiments                            agation and approximate inference in deep generative models,” in
on three variational inference methods to train our proposed                         Proceedings of the 31st ICML. JMLR. org, 2014, pp. II–1278.
NVISA model, targeted for the unsupervised clustering task.                     [12] A. Mnih and K. Gregor, “Neural variational inference and learning in
                                                                                     belief networks,” in ICML, 2014, pp. 1791–1799.
We showed that directly using qφ (z|y, x), instead of using                     [13] Y. Burda, R. Grosse, and R. Salakhutdinov, “Importance weighted
the mean-field approximation, can benefit the unsupervised                           autoencoders,” arXiv preprint arXiv:1509.00519, 2015.
clustering with Gaussian Mixture VAE. Moreover, we proposed                     [14] C. Robert and G. Casella, Monte Carlo statistical methods. Springer
                                                                                     Science & Business Media, 1999.
std annealing trick to stabilize the training process and help                  [15] M. D. Hoffman and M. J. Johnson, “Elbo surgery: yet another way to
form better embeddings in the latent space with different                            carve up the variational evidence lower bound,” in Workshop in Advances
variational inference methods.                                                       in Approximate Bayesian Inference, NIPS, 2016.
                                                                                [16] Y. LeCun, L. Bottou, Y. Bengio, P. Haffner et al., “Gradient-based
   NVISA overall outperforms all the baseline clustering                             learning applied to document recognition,” Proceedings of the IEEE,
algorithms on the five benchmark datasets. Specifically, we                          vol. 86, no. 11, pp. 2278–2324, 1998.
focus our discussions and the combinations of qφ (z|y, x), non-                 [17] H. Xiao, K. Rasul, and R. Vollgraf. (2017) Fashion-mnist: a novel image
                                                                                     dataset for benchmarking machine learning algorithms.
reparameterized variational inference method VIMCO and std                      [18] A. Coates, A. Ng, and H. Lee, “An analysis of single-layer networks in
annealing in the context of clustering problem, make NVISA                           unsupervised feature learning,” in Proceedings of the 14th international
significantly outperforms the previous Gaussian Mixture VAE-                         conference on artificial intelligence and statistics, 2011, pp. 215–223.
                                                                                [19] A. Torralba, R. Fergus, and W. T. Freeman, “80 million tiny images: A
based clustering methods on all the five datasets, which                             large data set for nonparametric object and scene recognition,” IEEE
highlights the benefit of our modifications to the Gaussian                          transactions on pattern analysis and machine intelligence, vol. 30, no. 11,
Mixture VAE. Additional experiments and analysis are taken                           pp. 1958–1970, 2008.
                                                                                [20] W. Hu, T. Miyato, S. Tokui, E. Matsumoto, and M. Sugiyama, “Learning
to show the effectiveness of each technique used in NVISA.                           discrete representations via information maximizing self-augmented
   One direction of future works is to theoretically under-                          training,” in ICML, 2017, pp. 1558–1567.
standing why std annealing can help learn separated embed-                      [21] X. Li, Z. Chen, L. K. M. Poon, and N. L. Zhang, “Learning latent
                                                                                     superstructures in variational autoencoders for deep multidimensional
dings in the latent space. Another direction is to incorporate                       clustering,” in 7th ICLR, 2019.
more powerful priors in NVISA, rather than using simple                         [22] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image
Gaussian mixtures (e.g., priors with complex structure like in                       recognition,” in Proceedings of the IEEE conference on computer vision
                                                                                     and pattern recognition, 2016, pp. 770–778.
LTVAE [21], or mixture of complex distributions derived by                      [23] J. Deng, W. Dong, R. Socher, L.-J. Li, K. Li, and L. Fei-Fei, “Imagenet:
flow-based models [30]). This can enhance the representation                         A large-scale hierarchical image database,” in 2009 IEEE conference on
capability of our model and may potentially further improve the                      computer vision and pattern recognition. IEEE, 2009, pp. 248–255.
                                                                                [24] H. W. Kuhn, “The hungarian method for the assignment problem,” Naval
clustering accuracy of NVISA on complex real-world datasets.                         research logistics quarterly, vol. 2, no. 1-2, pp. 83–97, 1955.
                                                                                [25] D. P. Kingma and J. Ba, “Adam: A method for stochastic optimization,”
               VI. ACKNOWLEDGEMENTS                                                  The International Conference on Learning Representations (ICLR), 2015.
  This work has been supported by National Key R&D                              [26] L. Theis, A. van den Oord, and M. Bethge, “A note on the evalua-
                                                                                     tion of generative models,” in International Conference on Learning
Program of China 2019YFB1802504, and Beijing National                                Representations (ICLR 2016), 2016, pp. 1–10.
Research Center for Information Science and Technology.                         [27] I. Higgins, L. Matthey, A. Pal, C. Burgess, X. Glorot, M. Botvinick,
                                                                                     S. Mohamed, and A. Lerchner, “beta-vae: Learning basic visual concepts
                             R EFERENCES                                             with a constrained variational framework,” in International Conference
 [1] J. MacQueen, “Some methods for classification and analysis of multi-            on Learning Representations, vol. 3, 2017.
     variate observations,” in Proceedings of the fifth Berkeley symposium on   [28] L. v. d. Maaten and G. Hinton, “Visualizing data using t-sne,” Journal
     mathematical statistics and probability, vol. 1, no. 14. Oakland, CA,           of machine learning research, vol. 9, no. Nov, pp. 2579–2605, 2008.
     USA, 1967, pp. 281–297.                                                    [29] B. Yang, X. Fu, N. D. Sidiropoulos, and M. Hong, “Towards k-
 [2] C. M. Bishop, Pattern Recognition and Machine Learning (Information             means-friendly spaces: Simultaneous deep learning and clustering,” in
     Science and Statistics). Berlin, Heidelberg: Springer-Verlag, 2006.             Proceedings of the 34th ICML. JMLR. org, 2017, pp. 3861–3870.
 [3] J. Xie, R. Girshick, and A. Farhadi, “Unsupervised deep embedding for      [30] D. P. Kingma and P. Dhariwal, “Glow: Generative flow with invertible 1x1
     clustering analysis,” in International conference on machine learning,          convolutions,” in Advances in Neural Information Processing Systems,
     2016, pp. 478–487.                                                              2018, pp. 10 236–10 245.
