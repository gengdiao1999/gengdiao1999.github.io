    LogParse: Making Log Parsing Adaptive through
                 Word Classification
        Weibin Meng1, 7 , Ying Liu2, 7 , Federico Zaiter1, 7 , Shenglin Zhang3 B, Yihao Chen1, 7 , Yuzhe Zhang3
          Yichen Zhu4 , En Wang5 , Ruizhi Zhang6 , Shimin Tao6 , Dian Yang6 , Rong Zhou6 , Dan Pei1, 7
                1 Department of Computer Science and Technology, Tsinghua University, Beijing 100084, China
                  2 Institute for Network Sciences and Cyberspace, Tsinghua University, Beijing 100084, China
                                   3 College of Software, Nankai University, Tianjin 300071, China
                             4 Department of Statistis, University of Toronto, Toronto M5S 1A1, Canada
                   5 College of Computer Science and Technology, Jilin University, Changchun 130012, China
                                                  6 Huawei, Beijing, 100084, China
        7 Beijing National Research Center for Information Science and Technology (BNRist), Beijing 100084, China

      mwb16@mails.tsinghua.edu.cn, liuying@cernet.edu.cn, zaitertf10@mails.tsinghua.edu.cn, zhangsl@nankai.edu.cn
                 chenyiha17@mails.tsinghua.edu.cn, zyzcs@mail.nankai.edu.cn, k.zhu@mail.utoronto.ca
       wangen@jlu.edu.cn, {zhangruizhi, taoshimin, yangdian4, joe.zhourong}@huawei.com, peidan@tsinghua.edu.cn


   Abstract—Logs are one of the most valuable data sources for
large-scale service (e.g., social network, search engine) mainte-
nance. Log parsing serves as the the first step towards automated
log analysis. However, the current log parsing methods are not
adaptive. Without intra-service adaptiveness, log parsing cannot
handle software/firmware upgrade because learned templates
cannot match new type of logs. In addition, without cross-
service adaptiveness, the logs of a new type of service cannot
be accurately parsed when this service is newly deployed. We
propose LogParse, an adaptive log parsing framework, to support
intra-service and cross-service incremental template learning and
update. LogParse turns the template generation problem into a
word classification problem and learns the features of template
words and variable words. We evaluate LogParse on four public
production log datasets. The results demonstrate that LogParse
supports accurate adaptive template update (increased from
0.559 to nearly 1.0 parsing accuracy), and a trained LogParse
is adaptive for a brand new service’s log parsing. Because of
LogParse’s adaptiveness, we also apply LogParse to an interesting
application, log compression and deployed log compression in a
top cloud service provider. We package LogParse into an open-
source toolkit.
   Index Terms—Log Analysis; Text Classification; Service Man-
agement; AIOps
                                                                          Fig. 1. Examples of network device logs and their templates
                        I. I NTRODUCTION
   Logs (see top half of Fig. 1), which record a vast range of
events of services (e.g., social network, datacenter devices),      they have to be properly parsed before they can be effectively
are one of the most valuable data sources for large-scale           used [9]. An unstructured log is essentially “printf”ed by
services maintenance [1]. Logs have been widely applied             services and thus follows some specific format: there is a
for monitoring status [2], [3], understanding events [4], [5],      template field sketching out the event and summarizing it,
detecting anomalies [6], [7], and predicting failures [8].          and a variable field varying from one log to another of the
   A large-scale service is often maintained by abundant oper-      same template. Take Fig. 1 as an example, in L1 , “ae3” is a
ators. For example, a social network application includes lots      variable word, whereas the rest, i.e., “Interface ..., changed
of softwares and underlying machines. Usually, an operator          state to up”, is the template field. There are some methods of
has incomplete information on the overall social network, and       document sumarization [10], sentence compression [11], [12]
none of them is familiar with all logs generated by this social     and clustering [13] in NLP domain. However, [14] proves that
network. Besides, service logs are usually unstructured texts,      NLP methods cannot parse logs accurately. To achieve the goal
                                                                    of automated log parsing, many rapid and accurate data-driven
  B Shenglin Zhang is corresponding author.                         template extraction approaches, e.g., Spell [15], LogSig [16]
                                                                                different template sets. As a result, current log extraction
                                                                                methods typically extract templates and build a dedicated
                                                                                template set for each service type. When a brand new service
                                                                                goes online, there are usually not enough historical logs to
                                                                                train accurate templates. For example, Fig. 2 and Fig. 3 show
                                                                                the accuracy of template extraction results when all logs and
                                                                                only 10% of logs are used for training respectively (detailed
                                                                                information is shown in Section IV). We observe that current
                                                                                template extraction methods achieve terrible accuracy when
Fig. 2. Accuracy of traditional template extraction results when all the logs   trained on a smaller sample of the logs. Nonetheless, as seen
are used for training
                                                                                in Fig. 2, current log parsing methods are accurate enough for
                                                                                historical logs.
                                                                                   Thus, log parsing methods without adaptiveness signifi-
                                                                                cantly limit many log analysis applications, because many
                                                                                applications require to have a corresponding template for any
                                                                                given logs. An example of this is Log compression, which is
                                                                                a useful application of log analysis. For instance, Google and
                                                                                Facebook respectively generate 100 Petabyte and 10 Petabyte
                                                                                of log data per month [19]. These massive logs, if not properly
                                                                                compressed, will consume immense storage space. However,
                                                                                traditional compression methods cannot compress and query
Fig. 3. Accuracy of traditional template extraction methods compared to
LogParse when only 10% of logs are used for training                            specific logs in real-time[20]. Fortunately, unstructured logs
                                                                                contain many redundant information. We can turn logs into
                                                                                “template index + variables” (e.g., “T1 + ae3” in Fig. 1), which
and IPLoM [17] have been proposed. However, the current log                     saves substantial storage. Log compression based on templates
parsing methods are not adaptive. This adaptiveness consists                    also require templates for any given logs. In other words, it
of two aspects, i.e., intra-service adaptiveness and cross-                     needs an adaptive log parsing method.
service adaptiveness.                                                              The key intuition is based on the following observations:
   For intra-service, operators continuously conduct soft-                      The difference between template words (e.g., “Interface” in
ware/firmware upgrades on services and underlying machines                      Fig. 1) and variable words (e.g., “ae3”) are obvious, both
to introduce new features, fix bugs or improve performance                      within the same service and among different services.
(e.g, a social network service updates its applications), which                    We turn the template generation problem into a word
can generate new types of logs [18]. These logs, however,                       classification problem to determine whether a word belongs
cannot match any existing templates, and thus new templates                     to template words. Then, we get word labels by using any
have to be learned, which is called template update. To                         existing traditional template extraction methods. Next, we
demonstrate services do generate many new types of logs                         represent any word by using vectors and learn the features
at runtime, we set apart 10% of logs from four public log                       of template words and variable words. Finally, a new template
datasets (described in Section IV-A) and extract log templates                  can be generated by combining template words from a new
using five template extraction methods. Then, we use these log                  type of log. We face the following three challenges.
templates to match the remaining 90% of logs. Table I shows                        1. There is a massive number of words in logs, and many
the ratio of logs without corresponding existing templates                      of these words may appear only once. Representing and
in the remaining 90% of logs. The template set should be                        classifying all words accurately is a challenging task.
incrementally learned, otherwise, a new template set has to
                                                                                   2. Some words may be present in both template and variable
be learned from all the logs. Considering the large volume of
                                                                                fields, we need to classify them based on their context.
the long-period historical logs, this is a vast amount of work.
                                                                                   3. We have to get labels for training a word classifier,
In Fig. 1, for example, the service generates two new logs
                                                                                however, there are not labels for each word in raw logs.
(L5 and L6 ). We have to update the template set because L6
cannot match any existing template.                                                To address the above challenges, we propose LogParse, an
   Cross-service adaptiveness means that a model trained by                     adaptive log parsing framework. The contributions of LogParse
service A is also suitable for service B. Log parsing without                   are as follows.
cross-service adaptiveness makes it labor intensive and time                      •   LogParse learns new templates in an incremental man-
consuming to build and maintain a service type specific                               ner, and thus only new types of logs generated after
template set. In real-world service providers, different types                        software/hardware upgrades have to be matched to the
of services are different in log syntax/semantics. For example,                       updated template set, which greatly reduces the amount
in the same social network service, “add nodes” and “delete                           of work comparing with rematching all the historical
nodes” functions generate different types of logs and have                            logs to templates. LogParse improves parsing accuracy
                           TABLE I
      R ATIOS OF UNMATCHED LOGS IN REMAINING 90% OF LOGS

        Method HPC HDFS Zookeeper Hadoop
        LogSig     0.953     0.478       0.222         0.699
        IPLoM      0.025     3e-05       0.199         0.265
         Spell     0.940     0.832       0.876         0.992
         Drain     0.940     3e-05       0.198         0.173
        FT-tree    0.933     7e-05       0.596         0.366
                                                                             Fig. 4. Adaptiveness of LogParse. We train LogParse only by using logs of
      from 0.559 to nearly 1.0 when it updates templates                     service A. Intra-service adaptiveness means that it matches new types of logs
                                                                             generated by service A. Cross-service adaptiveness means we use it to match
      incrementally.                                                         real-time logs from a different service B.
   • Since LogParse is cross-service adaptive, it is general
      to diverse types of services/machines. This way, we
      build and maintain only one template set for all types                    Although FT-tree [22] and Drain [21] support incremental
      of services/machines, which saves a lot of time and                    learning, their performance in such task is bad (shown in Fig.
      resources.                                                             3). What’s more, there is no template extraction method with
   • LogParse is able to assign/generate templates for any                   cross-service adaptiveness.
      given logs, so that we can achieve novel log analysis ap-                 An adaptive log parsing method will improve the per-
      plications by adopting LogParse. For example, LogParse                 formance of many applications of log analysis (e.g., log
      can be easily applied for log compression.                             compression, anomaly detection, failure prediction base on un-
                             1                                               structured logs). For example, PreFix [8], a failure prediction
   • We have open-sourced LogParse, and hope that it can
      be used for future research.                                           system based on switch logs, cannot handle new types of logs.
   The rest of the paper is organized as follows: We discuss                 DeepLog [7] addressed new templates learning by obtaining
related works in Section II and propose our approach in                      operators’ feedback during anomaly detection. LogAnomaly
Section III. The evaluation is shown in Section IV. In Section               [6] is a state-of-the-art method to detect anomalies based on
V, we introduce LogParse’s applications. Finally, we conclude                unstructured logs. However, it deals with new types of logs
our work in Section VI.                                                      by merging them into the most similar existing templates.

                       II. R ELATED W ORK                                                                  III. D ESIGN
                                                                             A. Overview
   Service logs play an important role in service management.
Log parsing serves as the the first step towards automated log                  The objective of LogParse is to be adaptive in order to
analysis. To achieve the goal of automated template extraction,              support intra-service incremental learning and cross-service
many data-driven approaches have been proposed. There are                    learning. LogParse can incrementally update the template set
many categories of template extraction methods [9]. The first                and thus new types of logs can be matched to the updated
category is cluster-based methods, which log template forms                  template set, without having to relearn the whole template set
a natural pattern of a group of log messages. From this view,                and rematch all the historical logs. In addition, LogParse can
log parsing can be modeled as a clustering problem, such                     build the template set of a service based on the templates of
as LogSig [16]. Next is longest common subsequence. For                      another service, which can address the problem that previous
example, Spell [15] uses the longest common subsequence                      log extraction methods cannot accurately learn templates for
algorithm to parse logs in a stream. Iterative partitioning is               a newly deployed service because of insufficient amount of
used in IPLoM [17]. Some methods use heuristics to extract                   logs.
templates. As opposed to general text data, log messages have                   Fig. 4 shows adaptiveness of LogParse. For intra-service
some unique characteristics. Consequently, Drain [21] propose                adaptiveness, we get word labels by using any existing tradi-
heuristics-based log parsing methods. The final category is                  tional template extraction methods and train LogParse model
frequent items mining. Log templates can be seen as a set                    in step#1 of Fig. 4. Then, we utilize the trained LogParse to
of constant tokens that occur frequently in logs, such as FT-                match online logs of service A. If failed, it will learn a new
tree [22].                                                                   template and incrementally update the template set. For cross-
   However, for the intra-service scenario, most methods only                service adaptiveness, we use LogParse trained on service A
focus on template extraction while the other two parts of log                directly, and then, match online logs of service B.
parsing, i.e., template matching and update, which are of vital                 We show the detailed design of LogParse in Fig. 5. In
importance for log analysis, are not being properly combined                 the offline component, LogParse first extracts templates from
to allow for adaptiveness.                                                   historical logs (Section III-B). Then, it distinguishes template
                                                                             words from variable words based on templates learned from
 1 LogParse is available on Github: https://github.com/WeibinMeng/LogParse   historical logs. Regarding these word classes as labels for
                                                   Fig. 5. Detailed design of LogParse



training the word classifier, it then classifies words into tem-                                               null
plate or variable words using a binary classifier (Section III-D).
When real-time logs are generated, LogParse matches them
to templates (Section III-C). If a real-time log cannot match                                 Interface               Vlan-interface
any existing template, LogParse constructs vectors for each
word of this log, distinguishes between template words and                                    changed                       changed
variable words based on the trained word classifier, learns a
new template, and adds it to the template set (Section III-D).                                  state                        state
B. Template Extraction
                                                                                                 to                           to
   Template extraction has been widely studied in previous
works [9], [22], and it is not the main contribution of this
                                                                                    down                up        down               up
work. Its objective is to learn templates from logs, based on
the observation that a log is “printf”ed by services, and usually                        T1               T3           T2             T4
follows predefined structures. For example, the template of L1
in Fig. 1, i.e., “Interface ae3, changed state to down”, is T1 ,           Fig. 6. The prefix tree constructed for the template set in Fig. 1
i.e., “Interface *, changed state to down”.
   As shown in Fig. 2, any existing template extraction meth-
ods can accurately learn templates from long-period historical           For example, “Interface, changed, state, to, down” in Fig. 6
logs. Therefore, we can use the template set automatically            is a template. Note that we can incrementally add new tem-
learned by these methods to distinguish between template              plates to this prefix-tree in case that new templates are learned
words and variable words.                                             from new types of logs. In this way, a log can be represented
                                                                      with its corresponding template index and variable words. For
C. Template Matching
                                                                      example, L1 in in Fig. 1 can be represented with its template
   After template extraction, we get a template set from his-         index T1 and variable word “ae3”.
torical logs. Motivated by the extremely efficient performance           A real-time log can match existing template following
of the prefix-tree structure in packet forwarding [23], we            Algorithm 1. Firstly, we get the word list of the new log. Then,
build a prefix-tree for the template set to accelerate template       according to the order of word list, we find the word nodes
matching. For example, Fig. 6 shows a prefix-tree for the logs        from template tree (if they are in template tree). Finally, if we
in Fig. 1, where the templates with solid line boxes are existing     find a root-to-leaf path in existing template tree, the path is
templates. It is constructed as follows.                              the matched template, otherwise, template search algorithm 1
   • We build a null root of a prefix-tree.                           will return “NULL” and we need to generate a new template
   • We scan each existing template and sort a word list L by         for the new log (describe in Section III-D).
     the order of each word’s appearance in the template.
   • We insert L into the prefix-tree, where each node is
                                                                      D. Template Generation
     a word. If two templates share a common prefix (e.g.,               Learning a template from a new type of log equals to
     “Interface, changed, state, to” in T1 and T3 )), we only         classifying the words in the log into template words and
     create a new subtree for different words.                        variable words. In Fig. 1, for example, “Interf ace, changed”
   • Each root-to-leaf path of the prefix-tree is a template.         are template words, and “ae3, vlan22” are variable words.
Algorithm 1 Template Search
Input: A template set trie T ST of all extracted log templates,
    and a sequence LW S containing each word from a given
    log in order
Output: A matched template M T
 1: Let a state S record a matching state up to a given node of
    T ST with the pair RM S being the remanent sequence of
    LW S as each of its words are processed and M N being
    the node from T ST yet to be matched
 2: Set the root of T ST , and the whole LW S as the initial
    state of S
 3: Create a queue Q and enqueue CS
 4: while Q is not empty do
 5:    Dequeue a state from Q as the current state CS
 6:    if CS is valid and M N is a leaf node of T ST then                          Fig. 7. Example of feature vector
 7:       Let the traceback of M N up to the root of T ST be
          matched template M T
 8:       return M T                                              (with an average accuracy of 0.978), as shown in Fig. 2. In
 9:    end if                                                     this way, LogParse automatically gets the labels for template
10:    for each child C of M N do                                 words and variable words without any manual labelling, and
11:       if C in RM S then                                       thus solves the first challenge.
12:          Create a new current state N CS with the pair of        2) Word Representation: Most of approaches treat word
             the updated RM S by removing its first word and      representations as the cornerstone in natural language pro-
             C                                                    cessing. Though it is effective, word-level representation (e.g.,
13:          Enqueue N CS in Q                                    word embedding [24]) is inherently problematic: it assumes
14:       end if                                                  that each word type has its own vector that can vary inde-
15:    end for                                                    pendently [25]. However, new types of logs introduced by
16: end while                                                     software/firmware upgrades aiming to add new features or fix
17: return NULL {No existing template was matched}                previous bugs result in new templates being generated online.
                                                                  What’s more, most words only occur once in logs and out-of-
                                                                  vocabulary (OOV) new words could not be addressed online
In this way, we transform the template update problem into        using a word-level representation.
a word classification problem. However, word classification          To address this problem, we represent each word in a log
faces three challenges: (1) Training a word classifier needs a    using a character-level count vector, as shown in Fig. 7. We
large amount of labels. (2) New words can appear in new           assume that words which share common components (prefix,
templates. (3) A variable word can appear in a template           symbols, numbers) may be potentially related.
sometimes.                                                           Specifically, for a given word, we construct a vector based
   The template update of LogParse, as shown in the orange        on the number of each character in this word. The character-
modules of Fig. 5, is proposed to address the above three         level count vectors with fixed dimensionality can represent
challenges. It includes four steps, i.e., word labeling, word     any word because the set of different characters is fixed (e.g.,
representation, word classifier, and new template generation,     there are 128 different characters in ASCII). In this way, we
as follows. As aforementioned, existing template extraction       address the second challenge. In addition, we can find that
methods cannot update templates at runtime. In this paper, we     some variable words can also appear in templates. Therefore,
propose a novel log parse framework, LogParse, which can be       classifying a word into a template word or a variable word
combined with all existing template extraction methods and        just based on the word itself is not accurate, so the context of
enable them to update templates. The update procedures of         the word should be considered for its classification. Inspired
LogParse are shown in Fig. 5 (orange modules).                    by the n-gram concept, we classify a word by combining it
   1) Word Labeling: There are tens of thousands of templates     with its previous word. For a given word, as shown in Fig. 7,
in large service providers, and thus manually classifying         we concat the count vectors of this and its previous words to
template words and variable words so as to get labels for         construct its feature vector. Consequently, the third challenge
training a word classifier is nearly infeasible. To address       is addressed.
this problem, LogParse distinguishes template words from             3) Word Classifier: As mentioned above, LogParse trans-
variable words by combining historical logs and the template      forms the log template update problem into a word classi-
set automatically constructed by existing template extraction     fication problem. For each word, it constructs a vector by
methods. Based on extensive investigations, we find that the      combining the character-level count vector of this word and
template set learned by these methods are accurate enough         its previous one. We utilize SVM [26], a popular machine
learning method, to train a binary classifier. Note that, other                                     TABLE II
machine learning methods can also be used to train the binary                               D ETAIL OF THE DATASETS
classifier. Applying SVM is not one of our contributions.
   4) New Template Generation: In the online component,                  Datasets                Description                  # of logs
we search the prefix-tree constructed from the template set              HPC       High performance cluster      433,489
and match real-time logs to existing templates following                 HDFS    Hadoop distributed file system 11,175,629
Algorithm 1. If the new log matches no existing template,              ZooKeeper      ZooKeeper service           74,380
for each word in this log, we construct a feature vector by             Hadoop     Hadoop MapReduce job          394,308
combining the character-level count vector of this and its
previous word. Then, we classify each word by the trained
word classifier. Finally, we construct a template based on the          1) Datasets: We conduct experiments over four public
template words. In Fig. 1, for example, L6 cannot match any          log datasets from distributed systems, which are HPC [27],
existing template. According to the word classifier, LogParse        HDFS [28], ZooKeeper [27], and Hadoop [29]. The detailed
finds that the words “Vlan-interface”, “changed”, “state”, “to”,     information of these datasets is listed in Table II. For each
“up” are template words and thus inserts them into the prefix-       dataset, [9] sampled logs and manually labelled each log’s
tree to update the template set (T4 in Fig. 6).                      template, which serves as the ground truth for our evaluation.
                                                                        2) Template Extraction Methods: As aforementioned, Log-
E. LogParse’s Application in Log Compression                         Parse can incorporate any existing template extraction method,
   As for log compression, traditional compression tools can-        so template extraction (in the offline component) is not the
not decompress specific logs in real-time because they need to       main contribution of this work. To demonstrate the perfor-
decompress whole file chunks [20]. Considering that when a           mance of LogParse regardless of the method used, we have
failure occurs, operators tend to traceback the logs of similar      implemented five template extraction methods: FT-tree [22],
failures for quick mitigation, real-time querying is an essential    Drain [21], Spell [15], LogSig [16] and IPLoM [17] (see
requirement for the operators.                                       Section II for more details). The parameters of these methods
   Usually, the number of different templates are much smaller       are all set best for accuracy.
than that of different logs, and in a log the number of                 3) Evaluation Metrics: We apply Rand index [30] to
variable words are much smaller than that of all the words.          quantitatively evaluate the accuracy of template extraction.
Therefore the unstructured logs contain a lot of redundant           Rand index is a popular method for evaluating the similarity
information. A log can be easily compressed (matched) to,            between two data clustering techniques or multi-class classifi-
and quickly decompressed (recovered) from, its template and          cations. What’s more, Rand index is applied to evaluating
variables. In order to (de)compress every log, the template set      existing template extraction methods in the literature, such
should be kept up to date. Consequently, log parsing, which          as in [22], [31]. For each template extraction method, we
extracts templates from logs, matches logs to templates, and         evaluate its accuracy by calculating the Rand index between
maintains a template set continuously, is a promising direction      the manual classification results and the templates learned
to efficiently and effectively compress logs.                        by it. Specifically, among the template learning results of a
   In this paper, we apply LogParse on two storage scenarios:        specific method, we randomly select two logs, i.e., x and y,
short-term storage and long-term storage. For short-term stor-       and define T P, T N, F P, F N as follows. T P : x and y are
age (e.g., within one year), operators have to query logs in real-   manually classified into the same cluster and they have the
time. Traditional compression methods cannot decompress for          same template; T N : x and y are manually classified into
given logs in real-time. They are only suitable to long-term         different clusters and they have different templates; F P : x
storage. As is shown in Fig. 1, we utilize log templates             and y are manually classified into different clusters and they
and LogParse to parse real-time logs and save “template              have the same template; F N : x and y are manually classified
index + variables” to compress original logs. For long-term          into the same cluster and they have different templates. Then
storage, we adopt double compression to process logs. That           Rand index can be calculated using the above terms as
is, use traditional compression methods to compress “template        follows: Rand index = T P +TTN  P +T N
                                                                                                       +F P +F N .
index + variables” sequence, which will achieve the highest             4) Experimental Setup: We conduct experiments on a
compression ratio.                                                   Linux server with Intel Xeon 2.40 GHz CPU and 64G memory.
                                                                     We implement LogParse with Python 3.6 and have open-
               IV. E VALUATION OF L OG PARSE                         sourced LogParse on github2 .
A. Experiment Setting                                                B. Evaluation on Incremental Learning
   In this section, we evaluate the performance of LogParse in         LogParse can incorporate any existing template extraction
supporting intra-service adaptiveness and cross-service adap-        method to learn templates from logs. It generates word repre-
tiveness. The datasets, template extraction methods, evaluation      sentations for each word in the logs, and together with their
metrics and experimental setup of the experiments are as
follows.                                                              2 LogParse is available on Github: https://github.com/WeibinMeng/LogParse
Fig. 8. Accuracy of template extraction results with (w/) LogParse when 10%
logs are used for training

                                                                              Fig. 9. The template extraction accuracy of LogParse as the percentage of
                                                                              training data changes
labels learned by the template extraction method, it then trains
a word classifier. We find the accuracy of the word classifier
heavily relies on that of template extraction. Fig. 2 shows the               training data, Fig. 9 shows the template extraction accuracy
accuracy (in terms of Rand index) of Drain, Spell, IPLoM,                     (in terms of Rand index) of LogParse on the four datasets,
FT-tree and LogSig on the datasets of HPC, HDFS, Zookeeper,                   as the percentage of training data increases from 10% to
Hadoop, respectively. For each method, it learns templates                    90%, respectively. Henceforth, LogParse incorporates FT-tree
from each dataset and then matches each log in the dataset                    as the log extraction method since FT-tree is robust to diverse
to a template. The existing template extraction methods are                   types of logs. With LogParse automatically and continuously
highly accurate in learning templates from logs. Specifically,                updating templates for new types of logs, the accuracies
they achieve an average accuracy of 97.80%. Consequently,                     of template extraction are quite stable as the percentage of
we can directly use the automatic template extraction results                 training data changes. In other words, LogParse is robust to
of these methods to build word representations and train a                    different scales of training logs, and can achieve high template
word classifier.                                                              extraction accuracy when trained based on a small scale of
   As aforementioned, LogParse can incrementally update its                   training data.
template set in case that new types of logs are generated
because of software/firmware upgrade. This is really important                C. Evaluation on Supporting Cross-service Adaptiveness
because otherwise new types of logs cannot match any existing                    LogParse is cross-service adaptive, which enables it to
templates, and the long period (large volume) of historical logs              accurately learn templates from logs when a new type of
have to rematch the new template set, which costs tremendous                  service is newly deployed. Based on these templates, an
amounts of time and resources.                                                anomaly detection/prediction method can be trained, and the
   To demonstrate the performance of LogParse in supporting                   logs can be compressed, soon after the new type of service
incremental learning and simulate the launch of new services,                 is deployed. Since different datasets are usually different in
for each dataset, we apply each template extraction method                    syntax, we conduct a cross-dataset experiment to demonstrate
to learn templates from 10% of their logs. Based on these                     the performance of LogParse in the cross-syntax scenario. We
templates, LogParse distinguishes template words and variable                 therefore train LogParse based on the logs of one dataset
words, builds word representations, and trains a word classifier              (as shown in the upper part of Fig. 5) and match the logs
(see Fig. 5 for more details). After that, for the remaining 90%              of another dataset (as shown in the lower part of Fig. 5)
logs, LogParse first matches them to the existing templates,                  based on the trained model. Table III shows the accuracy
and if failed, it then builds their word representations, updates             (in terms of Rand index) of LogParse for the cross-dataset
templates based on the trained word classifier, and matches                   template learning and matching. LogParse achieves close-
them to the updated template set. If we only utilized template                to-one Rand index for all the cases. For example, when
extracting methods without update, as shown in Fig. 3, the                    LogParse is trained based on the HPC dataset and applied
average template extraction accuracy is 0.559. However, Fig. 3                to match the logs of the HDFS dataset, the Rand index is
also shows that, when LogParse is applied to update templates,                0.983. On average, LogParse achieves a cross-dataset accuracy
the template extraction accuracy on the remaining 90% logs                    of 0.980, which strongly demonstrates that LogParse is cross-
of each dataset is relatively high. Note that, the accuracy of                service adaptive.
LogParse in Fig. 3 is the average accuracy obtained on each
dataset as it is shown in Fig. 8. Clearly, the average template                          V. A PPLICATION IN L OG C OMPRESSION
extraction accuracy of each method on each dataset is 0.958.                     As aforementioned, LogParse, which is cross-service adap-
In other words, LogParse improves the template extraction                     tive and supports incremental learning, can be used to com-
accuracy by 71.5% when new types of logs are generated.                       press logs and quickly query (decompress) specific logs after
   To demonstrate how robust LogParse is to the scale of                      compression.
                           TABLE III                                                               TABLE IV
T HE ACCURACY OF L OG PARSE IN CROSS - DATASET TEMPLATE LEARNING       T HE COMPRESSION RATIOS OF L OG PARSE - ENABLED COMPRESSION
                         AND MATCHING                                        APPROACH , BZIP, 7 ZIP, ZIP AND THEIR COMBINATIONS


        Training            Testing data                                  Method         HPC HDFS ZooKeeper Hadoop Average
          data       HPC HDFS ZooKeeper Hadoop
                                                                        LogParse      23.4% 14.1%        10.9%      42.4% 22.7%
         HPC       -   0.983           0.999       0.923                  bzip         5.7% 9.3%         3.0%        5.6% 6.1%
        HDFS 0.982       -             0.993       0.974             LogParse w/ bzip 3.4% 3.2%           2.4%       3.6% 3.2%
       Zookeeper 0.993 1.0               -         0.937                  7zip         7.0% 8.5%         3.1%        5.0% 5.9%
        Hadoop 0.983 0.999             0.999         -               LogParse w/ 7zip 3.7% 6.3%           2.8%       3.4% 4.1%
                                                                           zip         8.6% 11.4%        4.8%        8.0% 8.2%
                                                                     LogParse w/ zip 4.4% 7.8%            2.8%       5.0% 5.0%
   A log can be represented by its template and variable words.
Since typically many logs share the same template, this repre-
                                                                                                TABLE V
sentation can greatly save storage space, and thus effectively        C OMPARISON OF THE QUERY TIME FOR ONE HUNDRED LOGS FROM THE
compress logs. Then the log can be quickly recovered from                          DATASET COMPRESSED FROM 1 TB LOGS
this representation. In this section, we show the performance of
LogParse in log (de)compression, with the public HPC, HDFS,            Method LogParse         bzip         7zip         zip
Zookeeper, and Hadoop datasets, and the logs collected from a            Time    0.212 ms 20.27 hours 1.12 hours 1.91 hours
top-tier global cloud service provider. We compare LogParse
with three commonly used compression methods, i.e., bzip
[32], 7zip [33], zip [34]. In addition, we apply the compression
ratio, i.e., size of compressed logs
               size of original logs , to evaluate the performance
                                                                     of LogParse for switch and firewall logs are 16.4% and 23.3%,
of each log compression method. Note that, accuracy of log           respectively. If LogParse is combined with bzip, the above
parsing doesn’t impact the log compression. This is because          compression ratios drop to 3.13% and 5.07%, respectively.
even if some templates are wrong, as variables are also saved           This service provider usually compress the logs per day,
together with template indexes, we can recover all compressed        which we believe, is not rare for other cloud service providers.
logs completely.                                                     In addition, the provider generates TBs of logs everyday, and
                                                                     operators usually have to query hundreds of logs to traceback
A. Evaluation on Public Datasets                                     a similar historical failure when a failure occurs. Therefore,
   TableIV shows the comparison ratios of LogParse-enabled           we compare the time needed for query 100 specific logs
compression approach, bzip, 7zip, zip, and their combinations.       from a dataset compressed from one TB logs. Table V shows
In LogParse-enabled compression, we simply apply LogParse            the comparison results. LogParse dramatically speeds up the
to compress template components, which can be quick de-              decompression and decreases the time from hours to 0.212 ms.
compressed. Then, we combine LogParse and traditional com-
pression approaches to achieve double compression. LogParse-                    VI. C ONCLUSION AND F UTURE W ORK
enabled compression approach achieves an average compres-
sion ratio of 22.7%. Given that the logs in the HPC and                 Logs play an important role in service maintenance and
Hadoop datasets usually have more variables, which are not           log parsing which is the first step of automated log analysis.
compressed in LogParse-enabled compression approach (using           However, current log parsing methods are not adaptive. We
“template + variable words” to represent a log), it has bad          propose a novel log parsing framework, LogParse, to parse
compression ratios on these datasets. For the historical logs        logs adaptively both in an intra-service and a cross-service
generated a long time ago, they are less likely to be queried,       manner. Our evaluation on four public production log datasets
and thus we can store them by combining LogParse-enabled             demonstrates LogParse improves parsing accuracy from 0.559
compression approach with the commonly used compression              to nearly 1.0 when new types of logs are generated. A trained
methods. For example, when we combine it with bzip, 7zip, or         LogParse is also adaptive for a brand new service’s log parsing.
zip, the combination achieves much smaller compression ratios        We have open-sourced LogParse, and hope that it can be used
than these commonly used methods, as shown in Table IV.              for future research. Besides, we demonstrate the power of
                                                                     LogParse when applied to log compression, enabling real-time
B. Deployment Experience                                             querying of compressed logs. Other interesting log analysis
  Considering the superior performance of LogParse in log            applications (e.g., log classification, log-based anomaly detec-
compression, we have deployed it in a top tier cloud service         tion, log-based failure prediction, log summary) with similar
provider to effectively parse and compress logs for network          requirements, where a template should always be assigned for
devices and firewalls. With LogParse, the service provider has       a given log, could now be enabled using LogParse. We will
represented (compressed) logs using the format of “template +        utilize LogParse to improve more log analysis applications in
variable words” since September 2018. The compression ratios         the future.
                      VII. ACKNOWLEDGMENT                                         [14] Weibin Meng, Ying Liu, Shenglin Zhang, Dan Pei, Hui Dong, Lei Song,
                                                                                       and Xulong Luo. Device-agnostic log anomaly classification with partial
  The work was supported by National Key R&D Program                                   labels. In 2018 IEEE/ACM 26th International Symposium on Quality of
                                                                                       Service (IWQoS), pages 1–6. IEEE, 2018.
of China (Grant No. 2018YFB1800405), the National                                 [15] Min Du and Feifei Li. Spell: Streaming parsing of system event logs.
Natural Science Foundation of China (Grant Nos. 61772307,                              In 2016 IEEE 16th International Conference on Data Mining (ICDM),
61902200, 61772230, 61972450 and 61402257), the China                                  pages 859–864. IEEE, 2016.
                                                                                  [16] Liang Tang, Tao Li, and Chang-Shing Perng. Logsig: Generating
Postdoctoral Science Foundation (Nos. 2019M651015,                                     system events from raw textual logs. In Proceedings of the 20th ACM
2017M611322 and 2018T110247) and the Beijing National                                  international conference on Information and knowledge management,
Research Center for Information Science and Technology                                 pages 785–794. ACM, 2011.
                                                                                  [17] Adetokunbo AO Makanju, A Nur Zincir-Heywood, and Evangelos E
(BNRist). We thank the anonymous reviewers for their                                   Milios. Clustering event logs using iterative partitioning. In Proceedings
valuable feedback. We thank Yixuan Zhang, Ya Su, Tianke                                of the 15th ACM SIGKDD international conference on Knowledge
Zhang for their helpful suggestions.                                                   discovery and data mining, pages 1255–1264. ACM, 2009.
                                                                                  [18] Shenglin Zhang, Ying Liu, Dan Pei, Yu Chen, Xianping Qu, Shimin
                                                                                       Tao, Zhi Zang, Xiaowei Jing, and Mei Feng. Funnel: Assessing
                              R EFERENCES                                              software changes in web-based services. IEEE Transactions on Services
                                                                                       Computing, 11(1):34–48, 2018.
                                                                                  [19] Gunasekaran Manogaran, Chandu Thota, and Daphne Lopez. Human-
 [1] Siddhartha Satpathi, Supratim Deb, R Srikant, and He Yan. Learning                computer interaction with big data analytics. In HCI challenges and
     latent events from network message logs: A decomposition based                    privacy preservation in big data security, pages 1–22. IGI Global, 2018.
     approach. arXiv preprint arXiv:1804.03346, 2018.                             [20] Danny Harnik, Ronen Kat, Dmitry Sotnikov, Avishay Traeger, et al. To
 [2] Tao Li, Yexi Jiang, Chunqiu Zeng, Bin Xia, Zheng Liu, Wubai Zhou,                 zip or not to zip: Effective resource usage for real-time compression. In
     Xiaolong Zhu, Wentao Wang, Liang Zhang, Jun Wu, et al. Flap: An                   USENIX FAST), pages 229–241, 2013.
     end-to-end event log analysis platform for system management. In             [21] Pinjia He, Jieming Zhu, Zibin Zheng, and Michael R Lyu. Drain:
     Proceedings of the 23rd ACM SIGKDD International Conference on                    An online log parsing approach with fixed depth tree. In 2017 IEEE
     Knowledge Discovery and Data Mining, pages 1547–1556. ACM, 2017.                  International Conference on Web Services (ICWS), pages 33–40. IEEE,
 [3] Subhendu Khatuya, Niloy Ganguly, Jayanta Basak, Madhumita Bharde,                 2017.
     and Bivas Mitra. Adele: Anomaly detection from event log empiricism.         [22] Shenglin Zhang, Weibin Meng, Jiahao Bu, Sen Yang, Ying Liu, Dan
     IEEE INFOCOM 2018 - IEEE Conference on Computer Communica-                        Pei, Jun Xu, Yu Chen, Hui Dong, Xianping Qu, et al. Syslog processing
     tions, 2018.                                                                      for switch failure diagnosis and prediction in datacenter networks. In
 [4] Shilin He, Qingwei Lin, Jian-Guang Lou, Hongyu Zhang, Michael R                   2017 IEEE/ACM 25th International Symposium on Quality of Service
     Lyu, and Dongmei Zhang. Identifying impactful service system prob-                (IWQoS), pages 1–10. IEEE, 2017.
     lems via log analysis. In Proceedings of the 2018 26th ACM Joint             [23] Keith Sklower. A tree-based packet routing table for berkeley unix. In
     Meeting on European Software Engineering Conference and Symposium                 USENIX Winter, volume 1991, pages 93–99, 1991.
     on the Foundations of Software Engineering, pages 60–70. ACM, 2018.          [24] Omer Levy and Yoav Goldberg. Neural word embedding as implicit
 [5] Tatsuaki Kimura et al. Spatio-temporal factorization of log data for              matrix factorization. In Advances in neural information processing
     understanding network events. In INFOCOM, 2014 Proceedings IEEE,                  systems, pages 2177–2185, 2014.
     pages 610–618. IEEE, 2014.                                                   [25] Dongyun Liang, Weiran Xu, and Yinge Zhao. Combining word-level
 [6] Weibin Meng, Ying Liu, Yichen Zhu, Shenglin Zhang, Dan Pei, Yuqing                and character-level representations for relation classification of informal
     Liu, Yihao Chen, Ruizhi Zhang, Shimin Tao, Pei Sun, et al. Loganomaly:            text. In Proceedings of the 2nd Workshop on Representation Learning
     Unsupervised detection of sequential and quantitative anomalies in                for NLP, pages 43–47, 2017.
     unstructured logs. In Proceedings of the Twenty-Eighth International         [26] Thorsten Joachims. Making large-scale svm learning practical. Technical
     Joint Conference on Artificial Intelligence, IJCAI-19. International Joint        report, Technical report, SFB 475: Komplexitätsreduktion in Multivari-
     Conferences on Artificial Intelligence Organization, volume 7, pages              aten, 1998.
     4739–4745, 2019.                                                             [27] Shilin He, Jieming Zhu, et al. Experience report: System log analysis
 [7] Min Du, Feifei Li, Guineng Zheng, and Vivek Srikumar. Deeplog:                    for anomaly detection. In 2016 IEEE 27th International Symposium on
     Anomaly detection and diagnosis from system logs through deep learn-              Software Reliability Engineering (ISSRE), pages 207–218. IEEE, 2016.
     ing. In Proceedings of the 2017 ACM SIGSAC Conference on Computer            [28] Wei Xu, Ling Huang, Armando Fox, David Patterson, and Michael
     and Communications Security, pages 1285–1298. ACM, 2017.                          Jordan. Largescale system problem detection by mining console logs.
 [8] Shenglin Zhang, Ying Liu, Weibin Meng, Zhiling Luo, Jiahao Bu, Sen                Proceedings of SOSP’09, 2009.
     Yang, Peixian Liang, Dan Pei, Jun Xu, Yuzhi Zhang, et al. Prefix: Switch     [29] Qingwei Lin, Hongyu Zhang, Jian-Guang Lou, Yu Zhang, and Xuewei
     failure prediction in datacenter networks. Proceedings of the ACM                 Chen. Log clustering based problem identification for online service
     on Measurement and Analysis of Computing Systems(SIGMETRICS),                     systems. In Proceedings of the 38th International Conference on
     2(1):2, 2018.                                                                     Software Engineering Companion (ICSE), pages 102–111. ACM, 2016.
 [9] Jieming Zhu, Shilin He, Jinyang Liu, Pinjia He, Qi Xie, Zibin Zheng,         [30] William M Rand. Objective criteria for the evaluation of clustering
     and Michael R Lyu. Tools and benchmarks for automated log pars-                   methods. Journal of the American Statistical association, 66(336):846–
     ing. In Proceedings of the 41st International Conference on Software              850, 1971.
     Engineering(ICSE), pages 121–130, 2019.                                      [31] Shenglin Zhang, Ying Liu, Weibin Meng, Jiahao Bu, Sen Yang,
[10] Qiang Jipeng, Qian Zhenyu, Li Yun, Yuan Yunhao, and Wu Xindong.                   Yongqian Sun, Dan Pei, Jun Xu, Yuzhi Zhang, Lei Song, and Ming
     Short text topic modeling techniques, applications, and performance: A            Zhang. Efficient and robust syslog parsing for network devices in
     survey. arXiv preprint arXiv:1904.07695, 2019.                                    datacenter networks. IEEE Access, 8:30245–30261, 2020.
[11] Liangguo Wang, Jing Jiang, Hai Leong Chieu, Chen Hui Ong, Dandan             [32] Julian Seward. b-zip. http://www.b-zip2.org/, 1996.
     Song, and Lejian Liao. Can syntax help? improving an lstm-based              [33] Igor Pavlov. 7-zip. http://www.7-zip.org/, 1999.
     sentence compression model for new domains. In Proceedings of the            [34] Phil Katz. zip. http://infozip.sourceforge.net, 1989.
     55th Annual Meeting of the Association for Computational Linguistics
     (Volume 1: Long Papers), pages 1385–1393, 2017.
[12] Thibault Fevry and Jason Phang. Unsupervised sentence compression
     using denoising auto-encoders. arXiv preprint arXiv:1809.02669, 2018.
[13] Jianhua Yin, Daren Chao, Zhongkun Liu, Wei Zhang, Xiaohui Yu, and
     Jianyong Wang. Model-based clustering of short text streams. In
     Proceedings of the 24th ACM SIGKDD International Conference on
     Knowledge Discovery & Data Mining, pages 2634–2642. ACM, 2018.
